# P1-01A — Arbor Repository & Toolchain Bootstrap 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立 Arbor 仓库的最小工具链骨架——严格、可复现的 build/typecheck/lint/test/start 全链路，不实现任何 P1 运行时行为。

**Architecture:** 单包 ESM 仓库，`tsc → dist/ → Node` 编译运行模型。五层目录骨架（domain/application/agent-runtime/infrastructure/entrypoints）+ 三级测试目录，全部为空壳（仅 .gitkeep），不含任何占位服务。依赖按"当前消费者"原则最小化安装。

**Tech Stack:** Node.js ≥24 / TypeScript（stable）/ pnpm / Vitest / Biome / Git。Effect 与 better-sqlite3 本 STEP 不安装（首个消费 STEP 引入，见 Task 2 说明）。

**权威文档（冲突时按此顺序）:**
1. `workspace_agent_stage_docs_v0_3/04_development/P1_01A_CODING_CONTRACT.md`（本 STEP 合同）
2. `workspace_agent_stage_docs_v0_3/00_pre_p1/P1_01_EXECUTION_BASELINE.md`
3. `workspace_agent_stage_docs_v0_3/00_pre_p1/PRE_P1_ENGINEERING_BASELINE.md`
4. `workspace_agent_stage_docs_v0_3/00_pre_p1/DEPENDENCY_RULES.md`

**已冻结的落地决策（本计划新增，记入 D-029）:**
- 仓库位置：`/home/lgao/work/arbor`（独立仓库）
- 版本对齐：文档 pin 的精确版本号若 npm 不存在，按 Task 1 的探测规则取真实版本，回填 `PRE_P1_ENGINEERING_BASELINE.md` 并在 `DECISION_REGISTER.md` 记 D-029
- 开发平台：Linux（本机）；代码保持可移植，Windows 验收留待后续（D-024 不变）

**背景校准（已验证）:** 文档 pin 的 `Effect 4.0.0-rc.113`、`TypeScript 7.0.2`、`pnpm 12.3.4`、`Vitest 5.0.0`、`better-sqlite3 13.0.3` 均为未验证版本号。opencode 仓库（2026 真实依赖快照）实际使用 `effect 4.0.0-beta.83`、`typescript 5.8.2` stable、`@types/node 24.12.2`。故 Task 1 必须先探测。

---

## 文件结构（本计划产出）

```text
/home/lgao/work/arbor/
├── .gitignore                  # Task 3
├── package.json                # Task 2
├── pnpm-lock.yaml              # Task 2 (pnpm install 生成)
├── tsconfig.json               # Task 3
├── biome.json                  # Task 3
├── vitest.config.ts            # Task 4
├── migrations/                 # Task 3 (仅 .gitkeep)
├── docs/
│   └── superpowers/plans/2026-09-14-p1-01a-bootstrap.md  # 本计划
├── src/
│   ├── domain/.gitkeep         # Task 3
│   ├── application/.gitkeep    # Task 3
│   ├── agent-runtime/.gitkeep  # Task 3
│   ├── infrastructure/.gitkeep # Task 3
│   └── entrypoints/
│       ├── version.ts          # Task 4（纯函数，可测）
│       └── main.ts             # Task 4（最小入口，调用 version.ts）
└── tests/
    ├── unit/
    │   └── smoke.test.ts       # Task 4
    ├── integration/.gitkeep    # Task 3
    └── acceptance/.gitkeep     # Task 3
```

`.gitkeep` 说明：合同要求目录骨架存在。空目录 git 不跟踪，用 `.gitkeep` 占位。这不是"placeholder service hierarchy"——没有代码，只有目录可见性。

---

### Task 1: 仓库初始化 + 依赖版本探测

**Files:**
- Create: `/home/lgao/work/arbor`（git 仓库本身）

- [ ] **Step 1: git init + 基线目录**

```bash
cd /home/lgao/work/arbor
git init
git symbolic-ref HEAD refs/heads/main
```

- [ ] **Step 2: 探测 npm 真实版本，产出版本表**

逐包执行 `npm view <pkg> dist-tags --json` 与 `npm view <pkg> version`，按以下规则确定版本并记录到 `docs/versions-P1-01A.md`（这张表就是后续所有版本引用的唯一来源）：

| 包 | 探测目标 | 决策规则 | 已验证 fallback |
|---|---|---|---|
| typescript | `dist-tags.latest` | 最新 **stable**（排除 `-dev`/`-rc`/`-beta`；若 latest 即 7.x stable 用 7.x） | `5.8.2` |
| vitest | `dist-tags.latest` | 最新 stable | 探测值（无独立 fallback，vitest 必装） |
| @biomejs/biome | `dist-tags.latest` | 最新 stable | 探测值 |
| @types/node | `dist-tags.latest` | 与本机 `node --version` 主版本一致的最新 | `24.12.2` |
| effect | `dist-tags` 全量 | 版本号最高的 `4.x`（beta/rc 均可）——**仅记录，本 STEP 不安装** | `4.0.0-beta.83` |
| better-sqlite3 | `dist-tags.latest` | 最新 stable——**仅记录，本 STEP 不安装** | 探测值 |
| pnpm | `pnpm --version`（本机）或 `dist-tags.latest` | 本机已装则用本机版本 | 探测值 |

同时记录本机 `node --version`、`git --version`、`pnpm --version`。

- [ ] **Step 3: 写版本记录文件**

创建 `docs/versions-P1-01A.md`，内容格式：

```markdown
# P1-01A 版本对齐记录 (YYYY-MM-DD)

| 包 | 文档 v0.3 pin | npm 实际采用 | 偏差 |
|---|---|---|---|
| typescript | 7.0.2 | <探测值> | <有/无> |
| effect | 4.0.0-rc.113 | <探测值> (未安装) | <有/无> |
| vitest | 5.0.0 | <探测值> | <有/无> |
| @biomejs/biome | 2.5.13 | <探测值> | <有/无> |
| better-sqlite3 | 13.0.3 | <探测值> (未安装) | <有/无> |
| pnpm | 12.3.4 | <探测值> | <有/无> |
| node | 24.21.0 LTS | <本机值> | <有/无> |
```

---

### Task 2: package.json + lockfile

**Files:**
- Create: `package.json`
- Create: `pnpm-lock.yaml`（install 生成）

依赖安装范围决策（依据合同 anti-abstraction 规则 + YAGNI）：
- **安装**: `typescript`、`vitest`、`@biomejs/biome`、`@types/node`（均为本 STEP 六条 pnpm 脚本的直接消费者）
- **不安装**: `effect`（首个消费者是 P1-02 Domain Core）、`better-sqlite3`（首个消费者是 P1-01 后续 runtime bootstrap STEP）。版本已探测记录，到时按 Task 1 的表引入。此选择写入完成报告 SHORTCUT AUDIT。

- [ ] **Step 1: 写 package.json**

版本号从 `docs/versions-P1-01A.md` 采用值填入（`<V:typescript>` 等为填入位置标记，执行时替换为探测值，不留尖括号）：

```json
{
  "name": "arbor",
  "version": "0.1.0",
  "private": true,
  "description": "Workspace-centric coding agent system",
  "type": "module",
  "packageManager": "pnpm@<V:pnpm>",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check .",
    "test": "vitest run",
    "start": "node dist/entrypoints/main.js"
  },
  "devDependencies": {
    "@biomejs/biome": "<V:@biomejs/biome>",
    "@types/node": "<V:@types/node>",
    "typescript": "<V:typescript>",
    "vitest": "<V:vitest>"
  }
}
```

- [ ] **Step 2: 安装并生成 lockfile**

```bash
cd /home/lgao/work/arbor && pnpm install
```

Expected: `Done in <N>s`，无 peer 警告级别错误；生成 `pnpm-lock.yaml` 与 `node_modules/`。

- [ ] **Step 3: 验证 lockfile 可复现安装**

```bash
rm -rf node_modules && pnpm install --frozen-lockfile
```

Expected: 成功（合同 Required behavior #1）。

- [ ] **Step 4: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "chore: package baseline with reproducible lockfile (P1-01A)"
```

---

### Task 3: 编译/lint/忽略配置 + 目录骨架

**Files:**
- Create: `tsconfig.json`
- Create: `biome.json`
- Create: `.gitignore`
- Create: `migrations/.gitkeep`、`src/{domain,application,agent-runtime,infrastructure}/.gitkeep`、`tests/{integration,acceptance}/.gitkeep`

- [ ] **Step 1: 写 tsconfig.json（compilerOptions 逐项来自 P1_01_EXECUTION_BASELINE §2，不得增删改）**

```json
{
  "compilerOptions": {
    "target": "ESNext",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "rootDir": "src",
    "outDir": "dist",
    "sourceMap": true,
    "declaration": false,
    "strict": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "useUnknownInCatchVariables": true,
    "verbatimModuleSyntax": true,
    "noImplicitOverride": true,
    "lib": ["ESNext"],
    "types": ["node"]
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "tests", "docs"]
}
```

说明：`lib`/`types` 是 NodeNext + Node 运行时的必需配套（非 strictness 削减）；`tests` 由 Vitest 自己转译，不进 tsc 产物边界。若 typecheck 因依赖包自带 `.d.ts` 报错，允许追加 `"skipLibCheck": true` 并在完成报告 ARCHITECTURE DEVIATIONS 记录——除此之外不得动任何选项。

- [ ] **Step 2: 写 biome.json（schema 版本 = Task 1 采用的 biome 版本）**

```json
{
  "$schema": "https://biomejs.dev/schemas/<V:@biomejs/biome>/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "linter": {
    "enabled": true,
    "rules": {
      "recommended": true
    }
  },
  "assists": {
    "actions": {
      "source": {
        "organizeImports": "on"
      }
    }
  }
}
```

注意：Biome 2.x 配置键若与上述有出入（如 `assists` vs `assist`），以 `pnpm biome explain` / 官方 2.x 文档为准修正键名，语义不变。

- [ ] **Step 3: 写 .gitignore（逐行来自 P1_01_EXECUTION_BASELINE §12）**

```gitignore
node_modules/
dist/
coverage/
.env
.env.*
!.env.example
*.log
.arbor/
```

- [ ] **Step 4: 建目录骨架**

```bash
mkdir -p migrations src/domain src/application src/agent-runtime src/infrastructure src/entrypoints tests/unit tests/integration tests/acceptance
touch migrations/.gitkeep src/domain/.gitkeep src/application/.gitkeep src/agent-runtime/.gitkeep src/infrastructure/.gitkeep tests/integration/.gitkeep tests/acceptance/.gitkeep
```

- [ ] **Step 5: 验证 typecheck 通过（此刻 src 无 ts 文件，tsc 应无错退出）**

```bash
pnpm typecheck
```

Expected: 退出码 0，无输出。

- [ ] **Step 6: Commit**

```bash
git add tsconfig.json biome.json .gitignore migrations src tests
git commit -m "chore: strict tsc/biome config and P1 directory skeleton (P1-01A)"
```

---

### Task 4: 最小入口 + smoke 测试（TDD）

**Files:**
- Create: `src/entrypoints/version.ts`
- Create: `src/entrypoints/main.ts`
- Create: `vitest.config.ts`
- Test: `tests/unit/smoke.test.ts`

- [ ] **Step 1: 先写失败测试**

`tests/unit/smoke.test.ts`：

```typescript
import { describe, expect, it } from "vitest"
import { arborVersion } from "../../src/entrypoints/version.js"

describe("smoke", () => {
  it("test harness executes and version constant is importable", () => {
    expect(arborVersion).toBe("0.1.0")
  })
})
```

`vitest.config.ts`：

```typescript
import { defineConfig } from "vitest/config"

export default defineConfig({
  test: {
    include: [
      "tests/unit/**/*.test.ts",
      "tests/integration/**/*.test.ts",
      "tests/acceptance/**/*.test.ts",
    ],
  },
})
```

- [ ] **Step 2: 跑测试确认失败**

```bash
pnpm test
```

Expected: FAIL——`Cannot find module '../../src/entrypoints/version.js'`。

- [ ] **Step 3: 最小实现**

`src/entrypoints/version.ts`：

```typescript
export const arborVersion = "0.1.0" as const
```

`src/entrypoints/main.ts`：

```typescript
import { arborVersion } from "./version.js"

export function main(): number {
  console.log(`arbor ${arborVersion} — P1-01A toolchain bootstrap OK (node ${process.version})`)
  return 0
}

main()
```

说明：`main.ts` 顶层调用 `main()` 是"最小可运行入口"的证据形态；不做参数解析、不做任何运行时功能（合同 Out of scope）。纯常量放独立文件是为了测试不引入 console 副作用。

- [ ] **Step 4: 跑测试确认通过**

```bash
pnpm test
```

Expected: PASS（1 test）。

- [ ] **Step 5: lint 通过（首次对真实代码跑 biome）**

```bash
pnpm lint
```

Expected: `Checked N files in <T>ms. Found 0 errors.` 若有格式报错，跑 `pnpm biome format --write .` 修复后重跑（不改 lint 规则）。

- [ ] **Step 6: Commit**

```bash
git add vitest.config.ts src/entrypoints/version.ts src/entrypoints/main.ts tests/unit/smoke.test.ts
git commit -m "feat: minimal compiled entrypoint and vitest smoke test (P1-01A)"
```

---

### Task 5: 六条命令全链路验证（合同 Required behavior）

**Files:** 无新增——本任务是验收。

- [ ] **Step 1: install（复现性已在 Task 2 验证，跳过重复）**

- [ ] **Step 2: typecheck**

```bash
pnpm typecheck
```

Expected: 退出码 0。

- [ ] **Step 3: lint**

```bash
pnpm lint
```

Expected: 0 errors。

- [ ] **Step 4: test**

```bash
pnpm test
```

Expected: `Test Files 1 passed`。

- [ ] **Step 5: build**

```bash
pnpm build && ls dist/entrypoints/
```

Expected: `dist/entrypoints/main.js`、`main.js.map`、`version.js`、`version.js.map` 存在（合同 #5：runnable ESM under dist/）。

- [ ] **Step 6: start（跑编译产物，不是 ts 源码）**

```bash
pnpm start
```

Expected stdout: `arbor 0.1.0 — P1-01A toolchain bootstrap OK (node v24.x.x)`，退出码 0（合同 #6）。

- [ ] **Step 7: 源码→产物隔离检查**

```bash
rm -rf dist && pnpm start; echo "exit=$?"
```

Expected: 报 `Cannot find module` 且 `exit≠0`——证明 `start` 确实依赖编译产物而非意外路径。检查后 `pnpm build` 恢复。

---

### Task 6: 回填文档 + D-029 + 完成报告

**Files:**
- Modify: `/home/lgao/work/research/opencode-dev/workspace_agent_stage_docs_v0_3/00_pre_p1/PRE_P1_ENGINEERING_BASELINE.md`（版本 pin 表替换为探测值）
- Modify: `/home/lgao/work/research/opencode-dev/workspace_agent_stage_docs_v0_3/05_governance/DECISION_REGISTER.md`（追加 D-029）
- Modify: `/home/lgao/work/research/opencode-dev/workspace_agent_stage_docs_v0_3/05_governance/CURRENT_EFFECTIVE_FACTS.md`（Toolchain pins 节同步）

- [ ] **Step 1: PRE_P1_ENGINEERING_BASELINE.md 版本表替换**

把 §1 的 pin 块替换为实际采用值（保留原值在注释行中以便追溯）：

```text
Node.js             <本机 24.x>
TypeScript          <采用值>
Effect              <探测值>   (未安装于 P1-01A；首个消费 STEP P1-02 引入)
pnpm                <采用值>
Vitest              <采用值>
@biomejs/biome      <采用值>
better-sqlite3      <探测值>   (未安装于 P1-01A；首个 runtime bootstrap STEP 引入)

<!-- v0.3 original pins (fictional/non-verified): TS 7.0.2, Effect 4.0.0-rc.113,
     pnpm 12.3.4, Vitest 5.0.0, Biome 2.5.13, better-sqlite3 13.0.3, Node 24.21.0 -->
```

- [ ] **Step 2: DECISION_REGISTER.md 追加**

```markdown
## D-029 P1 toolchain version alignment
Status: Accepted

P1-01A discovered that several v0.3 pins reference non-existent npm versions.
Actual available versions were adopted, recorded in docs/versions-P1-01A.md
(Arbor repo), and backfilled into PRE_P1_ENGINEERING_BASELINE.md.
effect and better-sqlite3 are deferred to their first consuming STEP.
```

- [ ] **Step 3: CURRENT_EFFECTIVE_FACTS.md 的 Toolchain pins 节同步为相同值**

- [ ] **Step 4: 提交 Arbor 仓库最终状态**

```bash
cd /home/lgao/work/arbor
git add -A
git commit -m "docs: P1-01A version alignment record (D-029)"
```

- [ ] **Step 5: 按合同格式输出完成报告**

输出（不写入仓库，作为 STEP 交付物呈现给用户）：

```text
STATUS / ARCHITECTURE BASIS / IMPLEMENTED / NOT IMPLEMENTED / FILES CHANGED /
TESTS (逐条 command+result) / BEHAVIORAL EVIDENCE (start 的实际 stdout) /
ARCHITECTURE DEVIATIONS / SHORTCUT AUDIT (future-phase abstractions: 无;
placeholders: 仅 .gitkeep; TODO/FIXME: 无; strictness weakened: 无;
unexpected dependencies: 无) / OPEN ISSUES / COMMIT (git log --oneline)
```

---

## 自审记录

- **Spec coverage**（对照 P1_01A 合同）: In scope 五项 → Task 2/3/4；六条 Required behavior → Task 2/5；Required tests → Task 4；Forbidden shortcuts → 无 tsx、无 strictness 削弱、无 ESLint/Prettier、无 monorepo、无占位服务（.gitkeep 已辩护）；Completion report → Task 6 Step 5。Out of scope 各项（Runtime Project/ARBOR_HOME/Git adapter/SQLite/...）无任何任务触碰 ✓
- **占位符扫描**: `<V:...>` 均为 Task 1 探测规则定义的运行时填入值，规则确定、有 fallback，非 TBD ✓
- **类型一致性**: `arborVersion` 在 version.ts/main.ts/smoke.test.ts 三处一致（`as const` 字符串字面量）✓
