# P0-017 Result — Package DAG architecture tests

## Status

COMPLETE — `pnpm check` fully green.

## Source Contract

- DID v1.4 §10.4 Dependency Rules
- DID v1.4 §10.4.1 Allowed internal dependency matrix
- DID v1.4 §10.3 Effect Boundary
- DID v1.4 §14.5 criterion 14
- `AGENTS.md` hard rule 5

## Files

- `tests/architecture/package-dag.ts` — pure `ALLOWED_EDGES`, `checkEdges`,
  `checkDomainImports`.
- `tests/architecture/package-dag.test.ts` — real package scan + checker
  self-tests.
- `tests/architecture/placeholder.test.ts` removed.
- root `package.json` — added `@types/node` (test tooling for `node:fs`).

## Acceptance Evidence

- Real scan: `packages/*/package.json` internal `@arbor/*` edges conform to
  §10.4.1 (only `domain` exists and declares none).
- `domain` declares no internal dependency; `checkDomainImports` proves
  domain source imports only relative / `effect` / `node:` specifiers.
- Checker is **non-vacuous**: synthetic manifests prove it fails on a
  forbidden edge, an unknown package, a deep import, and an infrastructure
  import.
- Canonical location `tests/architecture/**` (DG-06).
- `pnpm architecture` and `pnpm check` green.

## Deviations

1. `@types/node` added as a devDependency so architecture tests can read
   manifests via `node:fs` (test tooling only; `domain` remains
   dependency-free).

## Design Gaps Discovered

None.

## Commit

`0aea03c` — test(p0-017): package DAG architecture tests
