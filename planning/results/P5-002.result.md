# P5-002 Result — Provisional RunnableWorkSource

## Status

COMPLETE — `pnpm check` green.

## Source

P5 `02`; DID v1.9 §8.18A, G2.

## Files

- `apps/single-workspace/src/runnable-source.ts` — `ProvisionalRunnableWorkSourceLive`.
- `apps/single-workspace/src/composition.ts` — wires it (replaces the P2 stub).
- `apps/single-workspace/test/p5-runnable-source.test.ts`.

## Acceptance Evidence

- `current` = workspace.currentWorkId when that Work is Open; `runnable` = Open Work minus current (stable order).
- Unset/terminal current → `None` + all Open Work.
- The P2 `RunnableWorkSource` port is unchanged; no dependency/global runnability (P7 supersedes).
- `pnpm check`: 95 files, 340 tests.

## Deviations

None.

## Commit

Recorded after commit.
