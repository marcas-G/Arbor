# P2-002 Result — P1/P0 Artifact Evolution + Pointer Notes

## Status

COMPLETE — `pnpm check` green.

## Source

P2 `01` §2.1–§2.3, §3; `02` §4; DID v1.7 G1.

## Files

- `packages/domain/src/command.ts` — `CommandSubmissionContext` gains `RecoveryController`.
- `packages/application/src/rejection.ts` — `CommandRejection` gains `ExecutionNotFound`.
- `packages/application/src/authority.ts` — `VerifiedRuntimeCommandAuthority`, `CommandAuthorityFact`, `StopAdmission` (ADT), origin-aware exact-match.
- `packages/application/src/gateway.ts` — `CommandHandler.stopAdmission`; `FenceStopCheck.check(context, stopAdmission)`; authority parameter → `CommandAuthorityFact`.
- `packages/application/src/commands/*.ts` — P1 handlers declare `Unclassified`.
- `packages/domain/test/command.test.ts` — `RecoveryController` case.
- `tests/p2-domain-evolution.test.ts`.
- P1 pointer notes: `01-command-contracts.md` §2/§3, `03-transaction-model.md` §4.

## Acceptance Evidence

- P1 test suite still green after the evolution.
- `ExecutionNotFound` is a `CommandRejection` but **not** a `DomainError` (type-level).
- `stopAdmission` is an ADT, not a boolean (type-level).
- `CommandAuthorityFact` accepts P1 and P2 facts; wrong `submissionOrigin` → `AuthorityDenied` reason.
- `pnpm check`: 40 files, 202 tests.

## Deviations

None.

## Commit

Recorded after commit.
