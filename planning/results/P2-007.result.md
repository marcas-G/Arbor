# P2-007 Result — `FenceStopCheck` Real Implementation

## Status

COMPLETE — `pnpm check` green.

## Source

P2 `02` §4, `03` §3–§5, `01` §3; DID v1.7 §9.7.

## Files

- `packages/execution-runtime/src/fence-stop.ts` — `FenceStopCheckLive` (authoritative, runs in the ambient command transaction).
- `packages/application/src/gateway.ts` — `FenceStopCheckService.check` E widened to `ExecutionRepositoryError`, R to `TransactionScope`; `CommandGatewayError` gains `ExecutionRepositoryError`.
- `tests/p2-fence-stop.test.ts`.

## Acceptance Evidence

- Missing lease / wrong generation / expired lease / settled execution → `FencingRejected`.
- `NormalExecutionMutation` + stop → `ExecutionStopping`; `QuiescenceControlMutation` and `StopControl` + stop → `Pass`.
- Non-`ExecutionOrigin` → `Pass`.
- `pnpm check`: 45 files, 215 tests.

## Deviations

- The hook error channel was widened to `ExecutionRepositoryError` (the real implementation reads the repository); recorded in the P2 contract index.

## Commit

Recorded after commit.
