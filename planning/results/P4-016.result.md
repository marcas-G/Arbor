# P4-016 Result — P2 `ReconciliationSource` + Stop/Quiescence

## Status

COMPLETE — `pnpm check` green.

## Source

P4 `06` §4, `02` §6; DID v1.8 §3.4; P2 `06` §5.

## Files

- `packages/tool-runtime/src/reconciliation.ts` — `ReconciliationSourceLive`.
- `adapters/persistence-sqlite/test/p4-reconciliation.test.ts`.

## Acceptance Evidence

- Only reconcilable unsettled invocations (`Reconcilable`/`NonIdempotent`/`Interrupted`) are reported; `ReadOnly`/`Idempotent` are excluded.
- NonIdempotent ambiguity → feeds P2 recovery's `OutcomeUnknown` decision; never blind replay.
- `pnpm check`: 91 files, 331 tests.

## Deviations

None.

## Commit

Recorded after commit.
