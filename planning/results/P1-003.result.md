# P1-003 Result — P1 DDL migrations

## Status

COMPLETE — `pnpm check` green.

## Source Contract

- `docs/design/implementation/P1/04-sqlite-schema.md` §3, §4, §6
- DID v1.6 §9.3, §9.13, §12.11

## Files

- `adapters/persistence-sqlite/src/migrations.ts` — `P1_MIGRATIONS` with the
  `0001_init` DDL: `projects`, `workspaces`, `sessions`, `works`,
  `resource_ownership`, `environment_revisions`, `commands`,
  `command_attempts`, `domain_events`, `project_event_sequences`,
  `consumer_offsets`, `consumer_dead_letters`.
- Deferred composite FKs (`(root_workspace_id, project_id)`,
  `(parent_workspace_id, project_id)`), `UNIQUE(workspace_id, project_id)`.
- CHECK constraints (lifecycles, resolution/result/error exclusivity,
  `released_at >= created_at`); partial active indexes.
- `adapters/persistence-sqlite/test/ddl.test.ts` — 4 tests.

## Acceptance Evidence

- Migration applies cleanly on an empty DB; all 12 tables exist.
- Deferred-FK bootstrap (project → root workspace → session) commits
  atomically.
- A root workspace from another project is rejected at COMMIT.
- `commands` resolution/result CHECK rejects an inconsistent row.
- `pnpm check`: 26 files, 138 tests.

## Deviations

1. The DDL is embedded as the TS constant `P1_MIGRATIONS` rather than separate
   `migrations/NNNN_*.sql` files; the migration-runner contract (user_version,
   forward-only, per-migration transaction) is unchanged.
2. Fence/stop predicates are documented in `04` §4 but the `executions` /
   `execution_leases` tables are P2.

## Design Gaps Discovered

None.

## Commit

Recorded after commit.
