# P0-012 Result — Acceptance / Deliverable / Dependency revision binding

## Status

COMPLETE — `pnpm check` fully green.

## Source Contract

- DID v1.4 §1.8 Dependency / Deliverable / Message
- DID v1.4 §3.6 Acceptance / Deliverable / Dependency revision binding
- DID v1.4 §12.8 C7 — Revision binding chain
- DID v1.4 §12.11 C10 — Dependency truth table & immutable durable records
- DID v1.4 §14.5 criterion 11

## Files

- `packages/domain/src/dependency.ts` — `DeliverableKind`, `ArtifactRole`,
  `ProducerBinding` (`AnyProducer` / `WorkspaceBound` / `WorkBound`),
  `ExpectedDeliverable`, `Deliverable`, `DeliverableMatchView`,
  `matchesExpectedDeliverable`, `Dependency`, `DependencyLifecycle`,
  `Acceptance`, transitions, and consequence functions.
- `packages/domain/src/index.ts` — re-export.
- `packages/domain/test/dependency.test.ts` — 8 tests.

## Acceptance Evidence

- Matcher algorithm exactly as frozen: producer binding, then kind, then all
  required artifact roles present. `ExpectedDeliverable` has no producer field.
- `SatisfyDependency` requires a matching view and binds
  `satisfiedAtDependencyRevision = dependency.revision`; mismatch returns
  `DependencyNotSatisfiable`.
- Terminal Dependency mutation (`Satisfied`/`Withdrawn`/`Unfulfillable`) is
  rejected with `TerminalLifecycleMutation` for satisfy/withdraw/unfulfill/
  revise.
- `reviseExpectedContract` requires authority and a new `DependencyRevision`.
- `Acceptance` binds `workId` + `targetWorkRevision` + `verificationId`
  (immutable readonly record).
- `withdrawDependenciesOnWorkCancel` withdraws only the cancelled consumer's
  `Unsatisfied` dependencies.
- `markUnfulfillableOnProducerLoss` follows `ProducerBinding`: `WorkBound` +
  `WorkCancelled` → `Unfulfillable`; `WorkspaceBound` + `WorkspaceRetired` →
  `Unfulfillable`; `AnyProducer` unchanged; one Work cancelled under a
  `WorkspaceBound` producer unchanged; replacement-in-same-governance-change
  unchanged.
- No `OutputContract` Domain Entity introduced.
- No infrastructure import in the domain package.

## Deviations

None.

## Design Gaps Discovered

None.

## Commit

`81732ac` — feat(p0-012): acceptance, deliverable & dependency revision binding
