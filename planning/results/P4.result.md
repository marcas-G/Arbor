# P4 Result — Tool Runtime

## Status

**COMPLETE — FORMALLY CLOSED.** `pnpm check` fully green. All 11 P4 exit
criteria evidenced. No open P4 Design Gap.

## Environment

Node 24.21.0 / pnpm 12.4.2 via `arbor-node24:24.21.0` + `env.sh`.

## Source Contract

- `docs/design/03-detailed-implementation-design.md` (DID v1.8) §11 P4, §1.5, §3.4, §6A.7, §7.2/§7.6/§7.8/§7.9, §9.12
- `docs/design/implementation/P4/**` (FROZEN, review Blocking=0)
- `planning/phases/P4.md` exit criteria

## Verification summary

| Command | Result |
|---|---|
| `arbor pnpm check` | PASS — lint + typecheck + architecture + test |
| `arbor pnpm test` | PASS — 93 files, 337 tests |
| `arbor pnpm architecture` | PASS — 26 tests |

## Exit criteria

1. `pnpm check` green — PASS (93 files, 337 tests; architecture 26).
2. `ToolCatalogPort` + full `ToolDefinition` with exact schemas — PASS (`p4-catalog`).
3. `ToolRuntimePort.invoke` implements the SD §6.5 pipeline with `Denied` + bounded observation — PASS (`p4-pipeline`).
4. Trusted `InvocationAuthority` exact-match + capability ceiling; Exact-Intent Approval atomic — PASS (`p4-authority`, `p4-approval`).
5. Validate-only resource admission — PASS (`p4-resource-admission`).
6. `SandboxPort` + minimal local executor — PASS (`p4-sandbox`).
7. `tool_invocations` intent-before-effect + `SideEffectSemantics` snapshot + `ReconciliationSource` — PASS (`p4-invocation-store`, `p4-reconciliation`).
8. Blob/Artifact service with bounded observation — PASS (`p4-artifact`).
9. Versioned `read`/`patch`/`shell` + shell policy enforcement — PASS (`p4-read`, `p4-patch`, `p4-shell`).
10. Architecture tests enforce P4 edges — PASS (`p4-architecture`).
11. No open P4 Design Gap — PASS.

## Frozen boundaries preserved

- Authority is a trusted `InvocationAuthority` fact; no PermissionGrant resolver, no default-allow.
- Exact-Intent `InvocationApproval` consumed atomically by exactly one invocation.
- Resource admission is validate-only; ownership changes remain governance Commands.
- `SandboxPort` + minimal local executor; advanced isolation deferred to P11.
- `read`/`patch`/`shell` are versioned contract artifacts with frozen schemas; shell policy enforcement is contract.
- Provider/tool settlement ADT + `SideEffectSemantics` drive retry/reconciliation; `NonIdempotent` ambiguity → `OutcomeUnknown`.

## Commits

```text
P4-001 526d4ed | P4-002 cb1f69e | P4-003 9fc2d0f | P4-004 ba7251a
P4-005 3cb7ace | P4-006 cfac556 | P4-007 12ed6a1 | P4-008 d1bbe10
P4-009 6a452ad | P4-010 4e24f98 | P4-011 67b5f71 | P4-012 ea1746c
P4-013/014/015 0b889b8 | P4-016 0eb507a | P4-017 4d2b1a2
```

## Deviations

- `InvocationAuthority.allowedCapabilities` + `ToolExecutionContext.authenticatedPrincipal` added for exact-match.
- `ToolIntent.approvalId` added for Exact-Intent Approval consumption.
- `ToolExecutor.requiresApproval` is a predicate (policy-driven for `shell`).

## Contract reconciliation (post-acceptance)

| Deviation | Owning contract |
|---|---|
| `InvocationAuthority.allowedCapabilities` | `P4/03` §2 |
| `ToolExecutionContext.authenticatedPrincipal` | `P4/01` §3 |
| `ToolIntent.approvalId` | `P4/01` §3 |
| per-intent approval predicate (`shell` destructive) | `P4/02` §2, `P4/08` §4 |

Targeted verification: `p4-authority` 2, `p4-approval` 2, `p4-pipeline` 5,
`p4-shell` 3, `p4-integration` 2, `p4-architecture` 4; `pnpm check` green
(93 files, 337 tests; architecture 26). P4 not reopened.

## Authorization

P4 COMPLETE / CLOSED. P5 is **not** authorized by this record; it requires P5
design closure (`docs/design/implementation/P5/**` exact contracts to
Blocking = 0).
