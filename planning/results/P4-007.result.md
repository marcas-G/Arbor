# P4-007 Result — `InvocationApproval` + Atomic Consumption

## Status

COMPLETE — `pnpm check` green.

## Source

P4 `03` §4; DID v1.8 G2; SD v1.3 §8.4.

## Files

- `packages/tool-runtime/src/approval.ts` — `matchApproval`, `actionDigestOf`.
- `adapters/persistence-sqlite/src/tool-invocations.ts` — `ToolInvocationStoreLive` (approval find/consume + intent/settle).
- `adapters/persistence-sqlite/test/p4-approval.test.ts`.

## Acceptance Evidence

- Exact intent → `Ok`; action-digest/expiry/consumed mismatch → `Denied`.
- `consumeApproval` is atomic: first true, second false (`UPDATE ... WHERE consumed_by IS NULL RETURNING`).
- `pnpm check`: 81 files, 312 tests.

## Deviations

- `ToolInvocationStoreLive` implemented here (the port is one service); P4-011 adds the intent-before-effect tests + `findUnsettled`.

## Commit

Recorded after commit.
