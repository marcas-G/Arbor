# P3-012 Result — `decodeTurn` + `AgentDirective` + Output Contract

## Status

COMPLETE — `pnpm check` green.

## Source

P3 `03` §3–§4, `01` §3; DID v1.7 §8.15/§8.3.

## Files

- `packages/model-context/src/decode.ts` — `AgentDirective` ADT, `OUTPUT_CONTRACTS`, `decodeTurn`, `ModelOutput`, `DecodedDirective`.
- `packages/model-context/test/p3-decode.test.ts`.

## Acceptance Evidence

- Text decodes into a `ModelOutput`.
- `ToolCallProposed` decodes into an `InvokeTool` directive (never the raw event); each directive carries `decisionBasisManifestId`.
- A directive not admitted by the Output Contract → `ModelOutputContractViolation`.
- Empty turn / unknown contract → violation.
- `pnpm check`: 68 files, 276 tests.

## Deviations

None.

## Commit

Recorded after commit.
