# P1-006 Result — CommandStore + DomainEventJournal

## Status

COMPLETE — `pnpm check` green.

## Source Contract

- `docs/design/implementation/P1/01-command-contracts.md` §2–§4
- `docs/design/implementation/P1/03-transaction-model.md` §3
- `docs/design/implementation/P1/04-sqlite-schema.md` §3.4, §3.5
- `docs/design/implementation/P1/05-event-journal.md`
- DID v1.6 §4.1, §9.9

## Files

- `adapters/persistence-sqlite/src/command-store.ts` — `CommandStoreLive`:
  `findResolution`, `insertCommitted`, `insertTerminalRejected`,
  `recordResolvingAttempt`, `recordRetryableAttempt`.
- `adapters/persistence-sqlite/src/journal.ts` — `DomainEventJournalLive`:
  `append` (project-local sequence upsert + EventId via IdGenerator),
  `readAfter`, `lastSequence` (0 when absent).
- `adapters/persistence-sqlite/test/command-store.test.ts`.

## Acceptance Evidence

- Committed row round-trips (fingerprint + result); attempts use
  `attempt_no` 0,1.
- First event of a new project allocates sequence 1; second 2; `lastSequence`
  = 2; `readAfter` returns both in order.
- `commands` persists only `Committed | TerminalRejected`.
- `command_attempts` has no FK; resolving attempt in the command tx, retryable
  in a separate scope.
- `pnpm check`: 29 files, 141 tests.

## Deviations

None.

## Design Gaps Discovered

None.

## Commit

Recorded after commit.
