# P1 Result — Persistence + Command Core

## Status

COMPLETE — `pnpm check` fully green. All 8 P1 exit criteria evidenced.
No open P1 Design Gap.

## Environment

Node 24.21.0 / pnpm 12.4.2 provided via the pinned image
`arbor-node24:24.21.0` and the project `env.sh` wrapper (host glibc 2.27
cannot run Node 24 natively).

## Source Contract

- `docs/design/03-detailed-implementation-design.md` (DID v1.6) §11 DID-10 (P1)
- `docs/design/implementation/P1/**` (FROZEN; P1-DG-01…11 RESOLVED)
- `planning/phases/P1.md` exit criteria
- `planning/README.md` conventions

## Verification summary

| Command | Result |
|---|---|
| `arbor pnpm check` | PASS — lint + typecheck + architecture + test |
| `arbor pnpm test` | PASS — 38 files, 194 tests |
| `arbor pnpm architecture` | PASS — 12 tests |
| `arbor pnpm typecheck` | PASS — `tsc -b && tsc -p tsconfig.test.json` |

Per-suite (vitest filter → passed):

```text
migrate 3 | runtime 1 | ddl 4 | transaction 1 | repositories 1
command-store 1 | ownership 4 | p1-journal-consumer 4 | p1-gateway 11
p1-create-project 3 | p1-create-child-workspace 4 | p1-assign-work 6
p1-idempotency 5 | p1-recovery 10 | p1-harness 2 | result-record 3
```

## Exit criteria

### Criterion 1 — `pnpm check` is green (lint + typecheck + architecture + test)
- status: PASS
- evidence: `arbor pnpm check` exit 0; 38 test files, 194 tests.

### Criterion 2 — P1 DDL migrates and all P1 repositories/ports are implemented per `02`/`04`
- status: PASS
- evidence: `adapters/persistence-sqlite` DDL (12 tables, `P1_MIGRATIONS`),
  migration runner (`migrate` 3, `ddl` 4); `packages/ports` contracts;
  Project/Workspace/Work/Session + ResourceOwnership + EnvironmentRevision
  repositories (`repositories` 1, `ownership` 4).

### Criterion 3 — `CommandGateway` implements the `01` pipeline
- status: PASS
- evidence: `packages/application/src/gateway.ts` — canonical v1 + SHA-256
  fingerprint, single-transaction resolution, idempotency
  (`IdempotencyConflict`), authority exact-match, terminal rejection, events;
  `p1-gateway` 11.

### Criterion 4 — `CreateProject` / `CreateChildWorkspace` / `AssignWork` match `01`
- status: PASS
- evidence: `p1-create-project` 3, `p1-create-child-workspace` 4,
  `p1-assign-work` 6 (payload/result/rejection/event per §5–§7).

### Criterion 5 — Idempotency/replay + concurrent-duplicate + recovery matrix tests pass
- status: PASS
- evidence: `p1-idempotency` 5 (same/different fingerprint, concurrent
  duplicates, operational retry → new attempt); `p1-recovery` 10 (C1–C11,
  P1–P4, ownership, migration).

### Criterion 6 — The fence/stop hook is present and stub-tested (inert for External/System)
- status: PASS
- evidence: `FenceStopCheck` service + `FenceStopCheckInertLive`;
  `p1-gateway` distinguishes `FencingRejected` from `ExecutionStopping` for
  `ExecutionOrigin`; inert default.

### Criterion 7 — Architecture tests enforce the new package edges (DID §10.4.1)
- status: PASS
- evidence: `tests/architecture/package-dag.test.ts` scans `packages/` and
  `adapters/`, adds `persistence-sqlite`/`environment-local` edges, and scans
  for catch-all errors / service locators (`pnpm architecture` 12).

### Criterion 8 — No open P1 Design Gap
- status: PASS
- evidence: `planning/gaps/README.md` — P1-DG-01…11 all RESOLVED; the
  convergence test asserts no OPEN `(?:P1-)?DG-\d+` row.

## Files changed (P1)

```text
packages/domain/src/{command,resources,...}.ts        (P1-018 evolution)
packages/ports/src/{command,consumer,environment,errors,journal,repositories,resources,runtime,session}.ts
packages/application/src/{authority,command-result,fingerprint,gateway,rejection}.ts
packages/application/src/commands/{create-project,create-child-workspace,assign-work,registry}.ts
adapters/persistence-sqlite/src/{client,migrate,migrations,repositories,transaction,command-store,journal,ownership,consumer,errors,index}.ts
adapters/environment-local/src/index.ts
tests/{p1-idempotency,p1-create-project,p1-create-child-workspace,p1-assign-work,p1-recovery}.test.ts
tests/support/p1-app.ts
tests/architecture/{package-dag.ts,package-dag.test.ts}
tests/convergence/result-record.test.ts
docs/design/implementation/P1/{00,01,02,03}.md         (P1-DG-11 closure)
planning/{README.md,phases/P1.md,gaps/*,tasks/*,results/*}
```

## Commits

```text
P1-017 792bb21 | P1-018 22ba90d | P1-002 118a684 | P1-001 21a3147
P1-003 7004c06 | P1-004 9090cca | P1-005 f32b34f | P1-006 3391898
P1-007 6c54c8b | P1-008 2dc54dd | P1-009 81ebb16 | P1-013 522c2b1
P1-015 e86661c | P1-DG-11 68866c7 | convergence 36593d0
P1-010 8c3992c | P1-011 d05a33d | P1-012 e4c1d2d | P1-014 01beecb
```

## Deviations

- P1-DG-11 resolved by manual governance as "authority as a trusted Application
  input fact" (P1 contracts 01 §2A, 02 §4A, 03 §3.1); no DID change.
- `01` §7 clarification: "work outside responsibility scope" is expressed by
  the authority fact; P1 does not evaluate the responsibility definition.
- `packages/application/tsconfig.json` adds `"types": ["node"]` for
  `node:crypto` (fingerprint hashing).
- `tests/` integration tests import sibling packages by relative source path
  (the root workspace has no `@arbor/*` links; the pinned offline package
  mirror rejects `pnpm install`).

## Discovered work / Design Gaps

None open. The Authority Resolver
(`Canonical facts + PermissionGrant + Parent/User governance + authenticated
Principal → Authority Resolver → VerifiedCommandAuthority`) is explicitly a
later-phase concern; P1 handlers need no change when it arrives.

## Authorization

P1 COMPLETE. P2 is **not** authorized by this record.
