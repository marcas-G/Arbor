# P0-015 Result — Pure authority / policy vocabulary

## Status

COMPLETE — `pnpm check` fully green.

## Source Contract

- DID v1.4 §1.6 Agent is not a long-lived Domain Entity
- DID v1.4 §1.1 `PermissionGrant`, `Decision`, `AgentBinding`, `AgentProfile`
- DID v1.4 §12.11 C10 — PermissionGrant truth table
- DID v1.4 §12.10 C9 — Permission row
- DID v1.4 §6.2 enforcement matrix
- DID v1.4 §14.5 criterion 13

## Files

- `packages/domain/src/authority.ts` — `ResponsibilityBoundAgentBinding`,
  `ExecutionBoundAgentBinding`, `AgentBinding`, `AgentProfile`,
  `PermissionGrant`, `grantPermission`, `revokePermission`, `Decision`,
  `recordDecision`.
- `packages/domain/src/index.ts` — re-export.
- `packages/domain/test/authority.test.ts` — 7 tests.

## Acceptance Evidence

- `AgentBinding` has exactly the two frozen variants; exhaustive switch with
  `never` default; the two variants are type-incompatible.
- `PermissionGrant` lifecycle matches §12.11: `GrantPermission → Active`;
  `RevokePermission → Revoked`; reactivating a `Revoked` grant is rejected
  with `TerminalLifecycleMutation`; revoke requires authority
  (`AuthorityDenied`).
- `Decision` supports supersede without in-place rewrite.
- Exported-symbol test: no `AgentId` / `Agent` aggregate exists.
- No infrastructure import in the domain package.

## Deviations

1. **Cross-task acceptance split.** The `Workspace.agentBinding:
   ResponsibilityBoundAgentBinding` assertion belongs to P0-006 (which
   depends on P0-015); P0-015 asserts the variant set and exhaustiveness.
   P0-006 will assert the Workspace field typing. Not a weakening.

## Design Gaps Discovered

None.

## Commit

`98d657b` — feat(p0-015): authority & agent-binding vocabulary
