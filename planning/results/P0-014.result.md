# P0-014 Result — Domain event envelope & catalog

## Status

COMPLETE — `pnpm check` fully green.

## Source Contract

- DID v1.4 §5.1 Event pipeline
- DID v1.4 §5.2 Event Envelope
- DID v1.4 §5.3 Core Event Catalog
- DID v1.4 §2.3 Event ordering & local ordinal

## Files

- `packages/domain/src/events.ts` — 33 catalog payload types, `EVENT_CATALOG`,
  `DomainEventPayload`, `DomainEvent<E>` envelope.
- `packages/domain/src/index.ts` — re-export.
- `packages/domain/test/events.test.ts` — 5 tests.

## Acceptance Evidence

- `EVENT_CATALOG` is exactly the 33 §5.3 event names.
- High-frequency runtime trace records (`ProviderTurn`, `ToolInvocation`,
  `LeaseRenew`, `StreamDelta`) are absent.
- Payload test: payloads carry a change fact (`_tag`) and no aggregate
  snapshot field (`lifecycle`, `responsibilityDefinition`, `currentWorkId`,
  `verdict`, `state`, `revision`, …).
- Envelope expressible and immutable; `eventType`/`sequence` correct.
- `EventId` / `EventSequence` are distinct (type-level `@ts-expect-error`).
- No infrastructure import in the domain package.

## Deviations

1. Catalog payloads are `_tag`-discriminated change-fact types; concrete
   per-event payload fields are phase-scoped (P1, §13).

## Design Gaps Discovered

None.

## Commit

`4d729d3` — feat(p0-014): domain event envelope & catalog
