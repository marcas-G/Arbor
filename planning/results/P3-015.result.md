# P3-015 Result — `DecisionStale` / Freshness

## Status

COMPLETE — `pnpm check` green.

## Source

P3 `06` §4; DID v1.7 §8.19.

## Files

- `packages/agent-runtime/src/freshness.ts` — `FreshnessRequirement`, `requirementForDirective`, `checkFreshness`, `DecisionStale`.
- `tests/p3-freshness.test.ts`.

## Acceptance Evidence

- Write/destructive directives (`InvokeTool`, `CompletionClaim`, …) → Strong; read-only → Weak.
- A changed Strong field → `DecisionStale` with the changed fields.
- Weak ignores non-environment changes but catches environment change.
- `None` never blocks.
- `pnpm check`: 71 files, 286 tests.

## Deviations

None.

## Commit

Recorded after commit.
