# P0 Result — Functional Domain Kernel

## Status

COMPLETE — `pnpm check` fully green. All 15 §14.5 criteria evidenced.

## Environment

Node 24.21.0 / pnpm 12.4.2 provided via the pinned image
`arbor-node24:24.21.0` and the project `env.sh` wrapper (host glibc 2.27
cannot run Node 24 natively). See `planning/results/P0-001.result.md`.

## Source Contract

- DID v1.4 §11 DID-10 (P0 scope)
- DID v1.4 §14.5 P0 completion criteria
- DID v1.4 §14.3 root acceptance entry
- `planning/README.md` conventions

## Verification summary

| Command | Result |
|---|---|
| `arbor pnpm check` | PASS — lint + typecheck + architecture + test |
| `arbor pnpm test` | PASS — 18 files, 121 tests |
| `arbor pnpm architecture` | PASS |
| `arbor pnpm build` | PASS |

## §14.5 criteria

### Criterion 1 — @arbor/domain compiles
- status: PASS
- evidence: `arbor pnpm build` (tsc -b) emits `packages/domain/dist`.

### Criterion 2 — Typed IDs cannot be crossed at compile time
- status: PASS
- evidence: `ids.test.ts` `@ts-expect-error` on `WorkspaceId → WorkId` and
  `ProjectId → WorkspaceId`; tests are compiled by `pnpm typecheck`.

### Criterion 3 — Core aggregate transitions are pure functions / typed results covering §12.11
- status: PASS
- evidence: Project/Workspace/Work/Execution/Verification/Dependency/
  PermissionGrant transitions in `packages/domain/src/*`; `invariants.test.ts`
  end-to-end `CompleteWork`.

### Criterion 4 — Work terminal lifecycle has no illegal reversal
- status: PASS
- evidence: `work.test.ts` rejects refine/cancel on terminal Work with
  `TerminalLifecycleMutation`.

### Criterion 5 — Parent is immutable
- status: PASS
- evidence: `workspace.test.ts`; `parentWorkspaceId` is a readonly field and
  no reparent/move/reactivate operation exists.

### Criterion 6 — CurrentWork ≤ 1 holds as domain semantics
- status: PASS
- evidence: single `currentWorkId` field; `selectCurrentWork` rejects foreign
  and non-Open candidates (`workspace.test.ts`).

### Criterion 7 — ResponsibilityDefinition / ResourceBoundary / ResourceAddress / CanonicalResourceRegion codecs and pure overlap algebra
- status: PASS
- evidence: `resources.test.ts` codec round-trips + algebra laws + cross-space
  non-overlap.

### Criterion 8 — Verification verdict ADT is fixed to PASS / FAIL / UNKNOWN
- status: PASS
- evidence: `verification.test.ts` exhaustive three-variant switch.

### Criterion 9 — Execution Binding ADT supports Work / Coordination and ExecutionBound
- status: PASS
- evidence: `execution.test.ts` admits `Work`/`Coordination` focus and
  `ExecutionBoundAgentBinding`.

### Criterion 10 — ExecutionSettlement is a legal sum type
- status: PASS
- evidence: `execution.test.ts` exhaustive settlement switch; type-level
  rejection of a `Completed` without `CompletedResult`.

### Criterion 11 — Acceptance / Deliverable / Dependency revision binding
- status: PASS
- evidence: `dependency.test.ts` binds `targetWorkRevision`,
  `sourceWorkRevision`, `satisfiedAtDependencyRevision`.

### Criterion 12 — CommandId / fingerprint / Resolution / Attempt; same-id different-fingerprint rejected
- status: PASS
- evidence: `command.test.ts` `IdempotencyConflict` on same id different
  fingerprint.

### Criterion 13 — No long-lived Agent Entity / AgentId
- status: PASS
- evidence: `authority.test.ts` / `invariants.test.ts` assert no `AgentId`
  symbol.

### Criterion 14 — §10.4.1 package DAG architecture tests pass
- status: PASS
- evidence: `tests/architecture/package-dag.test.ts` (non-vacuous checker).

### Criterion 15 — pnpm check is fully green
- status: PASS
- evidence: `arbor pnpm check` exit 0; 18 test files, 121 tests.

## Design Gaps

All six P0 Design Gaps (`DG-01` … `DG-06`) are `RESOLVED`. No `OPEN` gap
remains.

## Deviations (phase-level)

1. Node 24 provided via container (host glibc 2.27).
2. `typecheck` = `tsc -p tsconfig.test.json` (src + tests); `build` = `tsc -b`.
3. `@types/node` added as test tooling for architecture tests.
4. Verification convention `pnpm test <suite>` (not `pnpm test -- <suite>`).

## Commit

P0 implementation commits: `a64e320` … `0aea03c`.
Result record commit recorded after this file.
