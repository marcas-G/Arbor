# P5-006 Result — Yield → WorkWait → Wake

## Status

COMPLETE — `pnpm check` green.

## Source

P5 `04` §2; P2 `02`; DID §8.18A.

## Files

- `packages/execution-runtime/src/commands/settle-execution.ts` — `yieldedWorkId` now derives the `WorkId` from the Execution binding focus (was a P2 stub returning `null`).
- `apps/single-workspace/src/runnable-source.ts` — reuses an ambient `TransactionScope` when present (scheduler's `reevaluate` opens one) instead of nesting.
- `apps/single-workspace/test/p5-yield-wake.test.ts`.

## Acceptance Evidence

- A `Yield` directive settles the Execution `Completed(Yielded)` and registers a durable `work_waits` row **in the same settlement transaction**.
- With the WorkWait present, `scheduler.reevaluate` → `Idle`; after clearing it → `Admit`.
- `pnpm check`: 99 files, 344 tests.

## Deviations

`yieldedWorkId` derivation is a P2 stub completion required by P5 `04` §2 (WorkWait registered on Yield); recorded as a P5 integration note, not a design change.

## Commit

Recorded after commit.
