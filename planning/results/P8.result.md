# P8 Result Record — Agentic Verification

**Phase:** P8 · **Contracts:** `docs/design/implementation/P8/**` (FROZEN; DID v1.11 / GQ1–GQ8) · **Plan:** `planning/phases/P8.md` (12 tasks) · **Date:** 2026-09-21

## Verdict

**COMPLETE — 11/11 exit criteria PASS; no open Design Gap.**

## Exit criteria matrix (`planning/phases/P8.md`)

| # | Criterion | Evidence |
|---|---|---|
| 1 | `pnpm check` green | 142 files / 641 tests / 0 errors (lint+typecheck+architecture+test) |
| 2 | Migrations M-1..M-4 landed with regressions green | M-1 wake shape (scheduler.ts + WaitSpec semantics; P2/P3 suites green); M-2 structured criteria (P0/P1/P6 + placeholder migration; legacy typed-rejected); M-3 optional parentExecutionId (P2 suite extended; P6 specialist regression green); M-4 ExecutionSettled payload + workId (events frozen; consumer uses it) |
| 3 | Five commands, frozen rejection sets; one-Open typed; acceptance double-uniqueness | p8-start-verification (10), p8-conclude (11), p8-acceptance-commands (10) |
| 4 | Story A closes the chain; PASS releases channel-1 | p8-acceptance Story A (binding-triple assertions; PASS wake release; scheduler zero-calls for channel 2) |
| 5 | Story B/C: FAIL same producer; UNKNOWN first-class | p8-acceptance Stories B/C (zero writes to works; no auto-PASS; channel-2 routing) |
| 6 | Story D: refine invalidates; stale re-acceptance rejected | p8-acceptance Story D |
| 7 | Story E: mission enforcement; deliverable binding; environment when executable | p8-acceptance Story E + p8-start-verification |
| 8 | Story F: program gates; P14 read-only negative | p8-programs (22; 6-family registry; tamper/version gates; forbidden-phrase negatives) |
| 9 | Story G: owner snapshot; no auto-abort; orphan flow | p8-acceptance Story G (retire-doesn't-abort; Unknown(Orphaned) event; re-Start new identity; VerificationAlreadyOpen) |
| 10 | Architecture tests; consumers never bypass the gateway | p8-architecture (6; module placement; consumer source scan; M-3 note; dependency freeze) |
| 11 | No open P8 Design Gap | none raised; convergence note below |

## Task completion

| Task | Status | Suite |
|---|---|---|
| P8-001 domain evolution (M-1/M-2, Orphaned, enums, payloads, aggregation) | ✅ | p8-domain (9) |
| P8-002 DDL 4 tables + repositories | ✅ | p8-ddl (4) |
| P8-003 StartVerification | ✅ | p8-start-verification (10) |
| P8-004 Record/Conclude (verifier-only, aggregation, Orphaned) | ✅ | p8-conclude (11) |
| P8-005 AcceptWorkOutcome + CompleteWork | ✅ | p8-acceptance-commands (10) |
| P8-006 Consumer A | ✅ | p8-consumer-a (7) |
| P8-007 Consumer B | ✅ | p8-consumer-b (4) |
| P8-008 Verifier spawn (M-3, orphans) | ✅ | p8-verifier-spawn (6) |
| P8-009 dual-channel wake | ✅ | p8-wake-channels (7) |
| P8-010 P9 + P14 programs + M-2 placeholder | ✅ | p8-programs (22) |
| P8-011 acceptance + architecture + guards | ✅ | p8-acceptance (17), p8-architecture (6) |
| P8-012 convergence & result record | ✅ | this file |

## v1.11 fidelity (G1–G6)

- **G1** structured criteria + required-qualified aggregation (domain pure function; Conclude enforces coverage + evidence binding + verdict == aggregate).
- **G2** work-level VerificationChanged (any verdict releases; exact (workId, targetWorkRevision) equality); one-Open-per-revision via partial unique index + typed `VerificationAlreadyOpen`.
- **G3** both chain ends deterministic consumers; CompleteWork re-validates the seven-fold precondition every submission (consumer never exempts).
- **G4** owner snapshot persisted at Start; retirement never auto-aborts (Story G); new verification uses current owner.
- **G5** Orphaned path: explicit `OrphanConclusionAuthority` (converged naming — distinct from AcceptanceAuthority), Unknown-only pairing, evidence-precondition waiver, silent restart & identity reuse impossible.
- **G6** P14 owned here (read-only contract + version/hash/eval; P10/P12 consume).

## Deviations / convergence notes

1. **Authority convergence (execution-time)**: the parallel drafts reused `AcceptanceAuthority` for the Orphaned governance face; converged to a dedicated `OrphanConclusionAuthority` variant — mechanical, no semantics changed; `alsoTags` mechanism added to `CommandAuthorityRule` for the dual-face Conclude command.
2. **M-1 legacy WaitSpec rows**: persisted old-shape conditions (verificationId-keyed JSON) are treated as non-matching by the new exact-equality release check — migrated rows are re-registered by new Yields; no silent reinterpretation (P8-wake-channels test pins this).
3. **P6 registry tests scoped**: the two P6 program suites now filter to `p6-*` families (registry grew to 6); assertion strength unchanged (recorded by P8-010).
4. **Verifier idle loop / daemon**: verifier execution driving (WorkerDispatch Verifier kind) is wired at spawn level; a long-running daemon remains P12 composition surface (consistent with P7 note).

## Notes for downstream phases

- **P9 (recovery hardening)**: crash windows already closed deterministically (spawn replay via caller-preallocated ids; consumer idempotency via deterministic command ids; orphan detection via `checkOrphanedVerifiers`).
- **P10**: Attention presentation owns the orphan/vacant views; P14 program consumed read-only.
- **P11**: environment binding consumes EnvironmentRevisionStore; P8 never produces environment facts.
- **P12**: consumer loops (verification/completion) need offset-driven daemon wiring; single-command-face preserved.

## Verification

```bash
pnpm check                  # 641 tests green
pnpm test p8-acceptance     # Stories A–G (17)
pnpm test p8-architecture   # P8 edges (6)
pnpm architecture           # 51 architecture tests
```
