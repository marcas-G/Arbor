# P0-016 Result — C10 truth-table coverage & cross-aggregate invariant audit

## Status

COMPLETE — `pnpm check` fully green.

## Source Contract

- DID v1.4 §12.11 C10 — Aggregate transition truth tables
- DID v1.4 §14.5 P0 completion criteria 2–13
- DID v1.4 §6.2 Enforcement Matrix, L1 rows

## Files

- `packages/domain/test/coverage-matrix.ts` — machine-readable matrix (55
  entries: 43 §12.11 rows + 12 §14.5 criteria), `REQUIRED_IDS`,
  `checkCoverage`, `KNOWN_SUITES`.
- `packages/domain/test/coverage.test.ts` — checker + mutated-fixture tests.
- `packages/domain/test/invariants.test.ts` — cross-aggregate suite.

## Acceptance Evidence

- Coverage matrix is machine-readable and complete; `checkCoverage` reports
  no missing / `UNCOVERED` / blank / unknown `testRef` entries.
- Checker is non-vacuous: tests prove it fails on a missing row, an
  `UNCOVERED` row, and an unknown `testRef`.
- Cross-aggregate suite: `CompleteWork` end-to-end binding;
  `CancelWork → withdrawDependenciesOnWorkCancel`; producer loss →
  `markUnfulfillableOnProducerLoss`; `RetireWorkspace` full precondition set;
  typed-ID non-interchangeability; no `AgentId`; command
  same-id/different-fingerprint rejection.
- Single-aggregate rows reference their owning task suites (not duplicated).
- No infrastructure import in the domain package.

## Deviations

None.

## Design Gaps Discovered

None.

## Commit

`6ecacbd` — test(p0-016): coverage matrix & cross-aggregate invariants
