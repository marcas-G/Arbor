# P2-016 Result — Lease / Stop / Settle / Recovery Matrix

## Status

COMPLETE — `pnpm check` green.

## Source

P2 `03` (all), `05` §5, `06`; DID v1.7 §6.3, §6A.5, §6A.6.

## Files

- `tests/p2-recovery-matrix.test.ts`.
- `adapters/persistence-sqlite/src/repositories.ts` — `appendEntry` fence now enforces `expires_at > now` (matches P2 `03` §3).

## Acceptance Evidence

- Fenced Session append after lease expiry → `LeaseFencingRejected`.
- Stopped + `QuiescenceControlMutation` → `Pass`; stopped + `NormalExecutionMutation` → `ExecutionStopping`.
- Recovery settles the stopped execution deterministically.
- `pnpm check`: 55 files, 237 tests.

## Deviations

- `appendEntry` fence expiry enforcement (P2 `03` §3 correction).

## Commit

Recorded after commit.
