# P0-007 Result — Responsibility / resource ownership algebra

## Status

COMPLETE — `pnpm check` fully green.

## Source Contract

- DID v1.4 §1.5 ResponsibilityDefinition, ResourceBoundary & Resource Ownership
- DID v1.4 §12.9 C8 — Resource ownership algebra
- DID v1.4 §12.10 C9 — resource boundary / ownership row
- DID v1.4 §14.5 criterion 7

## Files

- `packages/domain/src/resources.ts` — `ResponsibilityDefinition`,
  `ResourceBoundary`, `ResourceAddress` (4 families), `CanonicalResourceRegion`,
  `RegionComparator`, `regionsOverlap`, `regionContains`, `sameSpaceConflict`,
  `ResourceOwnershipClaim`, `claimWithinBoundary`.
- `packages/domain/src/index.ts` — re-export.
- `packages/domain/test/resources.test.ts` — 7 tests.

## Acceptance Evidence

- Codecs round-trip for `ResponsibilityDefinition`, `ResourceBoundary`,
  `CanonicalResourceRegion`, and all four `ResourceAddress` variants.
- Different `resourceSpaceId` → `regionsOverlap = false` and
  `regionContains = false` (never throws, never true).
- Frozen algebra laws asserted against an in-memory **test** comparator
  (throwaway; no production comparator implemented): overlap symmetry,
  containment reflexive/transitive, containment implies overlap,
  non-overlap implies no conflict.
- Exclusive-write conflict is exactly `sameSpaceConflict`.
- `ResourceOwnershipClaim ⊆ ResourceBoundary` expressed as the pure
  `claimWithinBoundary` predicate.
- `normalizedRegion` is opaque (`Schema.Unknown`); P0 does not decide its
  encoding (P1, §13).
- No infrastructure import in the domain package.

## Deviations

1. `RegionComparator<R>` and `SpatialRegion<R>` are provided as the frozen
   protocol; the concrete region encoding is left to P1 per §13.

## Design Gaps Discovered

None.

## Commit

`e05121e` — feat(p0-007): responsibility & resource ownership algebra
