# P1-002 Result — `ports` package: Effect service contracts

## Status

COMPLETE — `pnpm check` green; `pnpm architecture` green.

## Source Contract

- `docs/design/implementation/P1/02-port-contracts.md`
- DID v1.6 §7.1–§7.4, §7.7, §10.4.1, §6A.2

## Files

- `packages/ports/src/session.ts` — `AdapterSession`, `TransactionScope`,
  `TransactionPort`, `TransactionOperationalFailure`.
- `packages/ports/src/errors.ts` — per-repository narrow errors (no universal
  `RepositoryError`), `EnvironmentError`, `ResourceResolutionStale`.
- `packages/ports/src/repositories.ts` — Project / Workspace / Work / Session /
  ResourceOwnership repositories.
- `packages/ports/src/command.ts` — `CommandStore`.
- `packages/ports/src/journal.ts` — `DomainEventJournal`, `PendingDomainEvent`.
- `packages/ports/src/consumer.ts` — `ConsumerOffsetStore`,
  `ConsumerDeadLetterStore`, `ProjectionStore`.
- `packages/ports/src/environment.ts` — `EnvironmentRevisionStore`,
  `ProjectEnvironmentPort`, `OwnershipWriteService`.
- `packages/ports/src/runtime.ts` — `Clock`, `IdGenerator`.
- `packages/ports/src/index.ts` — re-exports.
- `packages/ports/test/ports.test.ts` — service presence, no catch-all,
  type-level `R = TransactionScope`.

## Acceptance Evidence

- All frozen services compile with the declared `Effect<A, E, R>` signatures.
- Type-level test: a repository method's `R` requires `TransactionScope`.
- No universal `RepositoryError` export.
- `ports` depends only on `domain`; `pnpm architecture` green.
- `pnpm check`: 23 files, 130 tests.

## Deviations

None.

## Design Gaps Discovered

None.

## Commit

Recorded after commit.
