# P4-002 Result — `ports` P4 Contracts

## Status

COMPLETE — `pnpm check` green.

## Source

P4 `01` (all), `02` §2, `03`–`07`.

## Files

- `packages/ports/src/tool.ts` — `ToolDefinition`, `InvocationAuthority`, `ToolIntent`, `ToolExecutionContext`, `CanonicalToolObservation`, `ToolInvocationSettlement`, `SideEffectSemantics`, `ToolRuntimePort`, `ToolDefinitionStore`, `SandboxPort`, `ResourceAdmission`, `ToolInvocationStore`, `BlobStorePort`, `ArtifactMetadataRepository`, `ArtifactService`, `Artifact`.
- `packages/ports/src/errors.ts` — P4 port errors.
- `tests/p4-ports.test.ts`.

## Acceptance Evidence

- P4 service keys match the contracts; `Denied` is the denial projection.
- `SideEffectSemantics` frozen to four kinds.
- `pnpm check`: 76 files, 301 tests.

## Deviations

- `InvocationRef` reuses the P2 `execution.ts` definition.

## Commit

Recorded after commit.
