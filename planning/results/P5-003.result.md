# P5-003 Result — `DirectiveUnsupported` + Slice Directive Dispatch

## Status

COMPLETE — `pnpm check` green.

## Source

P5 `03`; DID v1.9 §8.15, G3.

## Files

- `packages/agent-runtime/src/directive.ts` — `DirectiveUnsupported`, `DirectiveOutcome`, `DirectiveHandler`.
- `packages/agent-runtime/src/driver.ts` — `AgentDriverLive(handlers)` dispatches decoded directives; unsupported kinds → `DirectiveUnsupported` Observation appended to the Session; the loop continues.
- `apps/single-workspace/src/composition.ts` — `ExecutionDriverPort` exposed; driver wired with `SessionRepository`/`TransactionPort`.
- `apps/single-workspace/test/p5-directive-unsupported.test.ts`.

## Acceptance Evidence

- `ProposeChildWorkspace` → non-fatal `DirectiveUnsupported` Observation; the loop continues and settles `Completed(Yielded)`.
- No `AuthorityDenied` / Execution failure for unsupported directives.
- `DirectiveUnsupported` is a directive execution result, not an `AgentDirective`/`DomainError`/`CommandRejection`.
- `pnpm check`: 96 files, 341 tests.

## Deviations

- `AgentDriverLive` became a function accepting directive handlers; P3/P4 callers updated to `AgentDriverLive()`.

## Commit

Recorded after commit.
