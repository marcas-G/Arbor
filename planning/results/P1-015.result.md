# P1-015 Result — Architecture Tests for New Packages

## Status

COMPLETE — `pnpm architecture` and `pnpm check` green.

## Source Contract

- DID v1.6 §10.4, §10.4.1, §10.3
- `tests/architecture/package-dag.ts` (P0-017)

## Files

- `tests/architecture/package-dag.ts` — added `persistence-sqlite` /
  `environment-local` to `ALLOWED_EDGES`; added `checkForbiddenPatterns`
  (catch-all error handling / service locator).
- `tests/architecture/package-dag.test.ts` — `readPackages` now scans
  `packages/` and `adapters/`; added per-package edge assertions and the
  forbidden-pattern scan (excluding `domain`).

## Acceptance Evidence

- `checkEdges` over `packages/*` + `adapters/*` = [] (no forbidden edge,
  unknown package, or deep import).
- `ports → domain`; `application → domain, ports`; each adapter →
  `domain, ports`.
- `domain` remains dependency-free and import-restricted.
- No `Effect.catchAll` / `Effect.catchAllCause` / `Context.unsafeGet` in any
  non-domain source; synthetic case flags a violation.
- Synthetic-manifest tests still fail on forbidden edges, unknown packages,
  and deep imports.
- `pnpm architecture`: 12 tests; `pnpm check`: 34 files, 167 tests.

## Deviations

None.

## Design Gaps Discovered

None.

## Commit

Recorded after commit.
