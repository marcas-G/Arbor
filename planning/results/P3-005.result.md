# P3-005 Result — Instruction Resolver + Trust Metadata

## Status

COMPLETE — `pnpm check` green.

## Source

P3 `02` §3–§4/§10; DID v1.7 §8.4A/§8.7.

## Files

- `packages/model-context/src/resolver.ts` — `resolveInstructions`, `InstructionConflict`, `GovernanceIssue`, `ResolvedInstructionSet`, `canonicalInstructionTrust`, `dataOnlyTrust`, `canRaiseAuthority`.
- `packages/model-context/test/p3-resolver.test.ts`.

## Acceptance Evidence

- Lower-authority fragment overridden by a Hard higher one → suppressed + `AuthorityConflict`.
- `ReplaceScope` on a non-replaceable slot → `ReplacementConflict`.
- Unresolvable same-level canonical conflict → `GovernanceBlocked` (`governanceIssues`).
- `DataOnly` trust cannot raise authority.
- `pnpm check`: 61 files, 257 tests.

## Deviations

None.

## Commit

Recorded after commit.
