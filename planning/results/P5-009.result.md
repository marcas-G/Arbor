# P5-009 Result — Full Vertical-Slice Acceptance Story

## Status

COMPLETE — `pnpm check` green. Unblocked by the P5-DG-01 resolution.

## Source

P5 `05`; DID v1.9 G1/G4/G6/G7.

## Files

- `apps/single-workspace/src/directives.ts` — `SliceDirectiveHandlersLive`
  (`InvokeTool` via P4, `Communicate`, `LoadSkill`, `ChangeMode`,
  `RequestGovernance`).
- `apps/single-workspace/src/loop.ts` — `evaluateAndSelect` (decision/execution
  split per P5 `01` §3.1) + `admitExecution`.
- `apps/single-workspace/src/registry.ts` — registers `SelectCurrentWork`.
- `packages/application/src/{authority.ts,commands/select-current-work.ts}` —
  the `SelectCurrentWork` canonical mutation command.
- `packages/agent-runtime/src/driver.ts` — persists the `ModelOutput` session
  entry.
- `apps/single-workspace/test/p5-slice-acceptance.test.ts` — the story.

## Acceptance Evidence (`05` §2)

- `CreateProject` + `AssignWork` commit; `reevaluate` → `SelectCurrentWork`;
  the loop forwards it to the Application command; `reevaluate` → `Admit Work`.
- Exactly one active main Execution at a time (`ActiveExecutionConflict`).
- Execution 1 settles `Completed(Yielded)`; Execution 2 settles
  `Completed(CompletionClaimed)`.
- Both Executions share the same `execution.sessionId`.
- Session entries include the `ModelOutput` + `Observation` (tool + unsupported).
- Work lifecycle remains `Open`; no verification table/row is created.
- A `WorkWait` exists between Yield and wake, then is cleared.
- Deterministic under the Fake Provider (no live network).
- `pnpm check`: 101 files, 347 tests.

## Deviations

- Resource ownership is seeded as a story precondition via the
  `ResourceOwnershipRepository` port: no P1–P4 command establishes ownership, so
  the story treats it as an out-of-slice governance fact (documented in the test).
- The tool turn uses `shell` with `cwd: FileTree "."`; the workspace boundary
  carries that address.

## Commit

Recorded after commit.
