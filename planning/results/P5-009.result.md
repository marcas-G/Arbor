# P5-009 Result — Full Vertical-Slice Acceptance Story

## Status

**BLOCKED** by `P5-DG-01` (OPEN). `pnpm check` green (346 passed, 1 skipped).

## Source

P5 `05`; DID v1.9 G1/G4/G6/G7.

## Delivered (unblocked parts)

- `apps/single-workspace/src/directives.ts` — `SliceDirectiveHandlersLive`
  implementing P5 `03` §2's directive set (`InvokeTool` via P4
  `ToolRuntimePort.invoke`, `Communicate`, `LoadSkill`, `ChangeMode`,
  `RequestGovernance`). This closes the P5-003 objective gap ("dispatch the
  slice's directive set"); P5-003 had delivered only the `DirectiveUnsupported`
  mechanism.
- `packages/agent-runtime/src/driver.ts` — the driver now appends the
  `ModelOutput` session entry that P3 `04` §3.4 defines and P5 `04` §10 /
  `05` §2 require.
- `apps/single-workspace/src/composition.ts` — wires the directive handlers into
  `AgentDriverLive`.
- `apps/single-workspace/test/p5-slice-acceptance.test.ts` — the full story
  (currently `it.skip` pending the gap).

## Blocker

After `CreateProject + AssignWork`, the frozen DID §8.18A / P2 `05` §4 table
returns `SelectCurrentWork(theOne)` (not `Admit Work(current)`) because
`workspace.currentWorkId` is unset and P1 commands do not set it. Executing
`SelectCurrentWork` is owned by P6/P7, and P5 must not decide it. Failure
evidence:

```text
$ arbor pnpm test p5-slice-acceptance
AssertionError: expected 'SelectCurrentWork' to be 'Admit'
```

See `planning/gaps/P5-DG-01-current-work-selection.md`.

## Verification

```bash
arbor pnpm test p5-slice-acceptance   # 1 skipped (blocked)
arbor pnpm check                      # green
```
