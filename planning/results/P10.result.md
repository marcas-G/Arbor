# P10 Result Record — Projection / UI

**Phase:** P10 · **Contracts:** `docs/design/implementation/P10/**` (FROZEN; DID v1.13 G1–G8 / GQ1–GQ7 + P7-GAP-01 rulings) · **Plan:** `planning/phases/P10.md` (13 tasks) · **Date:** 2026-09-22

## Verdict

**COMPLETE — 11/11 exit criteria PASS; P7-GAP-01 CLOSED; no open Design Gap.**

## Exit criteria matrix (`planning/phases/P10.md`)

| # | Criterion | Evidence |
|---|---|---|
| 1 | `pnpm check` green | 171 files / 840 tests / 0 errors |
| 2 | Event faces equipped; vocabulary frozen | p10-domain-events (5): HumanInterventionApplied payload exact; WorkSteered back-filled to P6 shape; ViewId 9-member union; 6-label status map incl. retired |
| 3 | SD inventory authoritative; statuses exhaustive | p10-tree-status (6): full label matrix + overlay; tree built from parent chains without per-node drill-in |
| 4 | Attention read-model (six sources, GAP-01) zero-mutation | p10-attention-readmodel (8) + Story B: severity/target/dedupKey per source; deadlock all-Action-Required (frozen gate); **GAP-01 WaitingOnVacantProducer live join with ¬Retired negative**; canonical snapshots unchanged |
| 5 | EffectiveFacts single ownership + freshness | p10-effectivefacts (10) + Story C: frozen input set; P3 single-source note; watermark/lag observable; barrier blocks/refuses; **no implicit RYW** (write-then-read lags; barrier forces convergence) |
| 6 | Detail/Verification/Dependency/CurrentWork/Transcript/Usage/Inbox views | p10-views-detail (7), p10-transcript-usage (4), p10-inbox-view (4) + Story D: first production read path over session_entries (paged, replay-stable cursor); usage observe-only; inbox watermark |
| 7 | Rebuild at scale | p10-rebuild (8) + Story E: three triggers, stage order raw→facts→tree→usage→attention-last, checkpoint resume along P1 offsets, RB-4 correctness, **journal horizon untouched (negative)**, Inbox reconciliation orchestration |
| 8 | HumanInterventionApplied provenance | p10-humanintervention (9) + Story F: steer two kinds paired same-transaction; four governance commands emit on human-originated submissions only; agent submissions emit nothing; Stop branch wire-only (resolver P12 precondition) |
| 9 | Action surfaces via gateway only | p10-actions (9) + Story G: Query message-mediated (P6 channel; no direct AdmitExecution); Steer/Stop/Governance entries; stop request validated by frozen P2 semantics |
| 10 | Architecture: boundaries enforced | p10-architecture + p10-boundaries (4): projection-runtime zero-write-face (SQL blacklist, P1-port whitelist); api-contracts domain-only; **no http/ws/cli anywhere (P12 transport negative)**; emit confined to application package; P14 consume-only |
| 11 | No open P10 Design Gap; GAP-01 closed | none raised; GAP-01 implemented as the canonical-state-derived view with zero runtime mutation (see below) |

## Task completion

| Task | Status | Suite |
|---|---|---|
| P10-001 domain/event-face evolution | ✅ | p10-domain-events (5) |
| P10-002 api-contracts + QueryPort + freshness types | ✅ | p10-api-contracts (18) |
| P10-003 tree/status | ✅ | p10-tree-status (6) |
| P10-004 attention read-model + GAP-01 | ✅ | p10-attention-readmodel (8) |
| P10-005 EffectiveFacts + freshness | ✅ | p10-effectivefacts (10) |
| P10-006 detail/verification/dependency/current-work | ✅ | p10-views-detail (7) |
| P10-007 transcript + usage | ✅ | p10-transcript-usage (4) |
| P10-008 inbox view + reconciliation | ✅ | p10-inbox-view (4) |
| P10-009 rebuild at scale | ✅ | p10-rebuild (8) |
| P10-010 human-intervention wiring | ✅ | p10-humanintervention (9) |
| P10-011 action surfaces | ✅ | p10-actions (9) |
| P10-012 acceptance + boundaries | ✅ | p10-acceptance (11), p10-boundaries (4) |
| P10-013 convergence & result record | ✅ | this file |

## P7-GAP-01 — CLOSED

Implemented exactly per the gap's disposition and DID v1.13 G8: the
`WaitingOnVacantProducer` Attention view is a **canonical-state-derived live
join** — `dependencies(Unsatisfied ∧ WorkspaceBound) × works(无 Open Work ∧
¬Retired)` — with **zero runtime mutation** (Story B canonical snapshot);
disposition remains governance commands only (S3 step 12). Positive and
negative fixtures (retired workspace with no Open Work does **not** report).
The gap file's non-descoping declaration is satisfied.

## DID v1.13 G1–G8 fidelity

- **G1** SD inventory authoritative: all must-views delivered; Search deferred with recorded rationale; Summary/Overview as Tree aggregations.
- **G2** EffectiveFacts single shared projection: P10-owned derive/materialization/query; P3 consumption note pinned; no second derivation anywhere.
- **G3** Attention read-model fully P10; upstream only produced facts (P7 deadlock, P9 escalation events consumed as-is); zero canonical mutations throughout.
- **G4** No resolver implemented; stop requests flow through frozen P2 validation; the dormant Stop emission branch is wire-only with its P12 precondition recorded.
- **G5** Watermark + observable lag + explicit barrier (minWatermark; maxLag as the noted contract-level extension); no implicit RYW (negative-tested).
- **G6** Programmatic contracts only: `api-contracts` package is the transport-neutral surface; zero http/ws/cli anywhere (boundary test).
- **G7** HumanInterventionApplied durable facts with AuthenticatedHuman provenance, same-transaction pairing; WorkSteered P6-shape back-fill landed as inherited evolution (P6 not reopened).
- **G8** See GAP-01 closure above.

## Deviations & notes

1. **Type placement (P10-002)**: signature vocabulary (FreshnessRequirement/QueryResult) lives in `domain`, port-error in `ports`, DTOs in `api-contracts` — keeps both DID §10.4.1 edges (ports=[domain], api-contracts=[domain]) intact without matrix changes.
2. **Read-only port extensions** (declared): SessionRepository.listEntries/listSessionsByWorkspace; DependencyRepository.listByProject; VerificationRepository.listByWork; MessageStore.listByRecipient; ProviderTurnStore.listUsageByProject.
3. **Regression alignment**: six earlier suites updated where paired intervention facts now accompany human-originated mutations (event-count 1→2 assertions).
4. **Usage cost = 0**: no canonical pricing source exists; tokens aggregated only (observe-only, invariant 45).

## Notes for downstream

- **P11**: environment/git unchanged by P10; EffectiveFacts consumers unaffected.
- **P12**: binds transports to `api-contracts`; implements the external authority resolver (the dormant Stop emission activates there); must not reinterpret view semantics; consumer-loop daemon wiring still pending from P8/P9 notes.

## Verification

```bash
pnpm check                # 840 tests green
pnpm test p10-acceptance  # Stories A–G (11)
pnpm test p10-boundaries  # boundary negatives (4)
pnpm architecture         # 68 architecture tests
```
