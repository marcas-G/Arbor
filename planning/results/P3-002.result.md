# P3-002 Result — `ports` P3 Contracts

## Status

COMPLETE — `pnpm check` green.

## Source

P3 `01` §1–§9, `02` §1/§7, `06` §1.

## Files

- `packages/ports/src/provider.ts` — `ProviderPort` (+ `PortableModelRequest`, `ProviderExecutionContext`, `CanonicalProviderEvent`, `ProviderFinishReason`, `ProviderFailureKind`), `ModelCapabilityPort`, `SkillRegistry`, `AgentContextSourcePort`, `KnowledgeQueryPort`, `ToolCatalogPort`, `SecretStorePort`, `InformationTrustMetadata`.
- `packages/ports/src/errors.ts` — `ProviderFailure`, `ModelCapabilityError`, `SkillRegistryError`, `ModelContextError`.
- `packages/ports/src/index.ts`; `tests/p3-ports.test.ts`.

## Acceptance Evidence

- P3 service keys match the contracts.
- `CanonicalProviderEvent` carries no `AgentDirective`.
- `ToolCatalogPort` contract declared (P4 owns implementation).
- `pnpm check`: 58 files, 247 tests.

## Deviations

- `modelRef` is a plain string (the domain has no `ModelRef` brand).

## Commit

Recorded after commit.
