# P3-004 Result — Prompt Program Contract Artifacts + Instruction Model

## Status

COMPLETE — `pnpm check` green.

## Source

P3 `05` §1–§4; DID v1.7 §8.4/§8.5/§8.6; C5.

## Files

- `packages/model-context/src/prompt.ts` — `PromptProgramFamily` (DID §8.4 P1–P14), `PromptProgram`, `PromptSlot`, `AuthorityRole` A0–A6, `CompositionMode`, `InstructionFragment`, `hashProgram`, and the four P3 programs (Base Agent Protocol, Responsibility-bound Protocol, Work Execution Program, Compaction Program).
- `packages/model-context/test/p3-prompt.test.ts`.

## Acceptance Evidence

- The four P3 programs expose revision/hash/slots/outputContractRefs/evalSetRef.
- `A0`–`A3` slots are non-replaceable and Hard.
- `hashProgram` is stable and revision-sensitive.
- `pnpm check`: 60 files, 253 tests.

## Deviations

None.

## Commit

Recorded after commit.
