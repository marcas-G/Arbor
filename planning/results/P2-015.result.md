# P2-015 Result — Recovery Skeleton + `RecoveryController`

## Status

COMPLETE — `pnpm check` green.

## Source

P2 `06` (all); DID v1.7 §3.4, §6A.6, §12.11; SD v1.3 §10.6–§10.7.

## Files

- `packages/execution-runtime/src/recovery.ts` — `runRecovery`, `ReconciliationSourceStubLive`.
- `tests/p2-recovery.test.ts`.

## Acceptance Evidence

- Expired leases invalidated; deterministic stop outcome settled via `SettleExecution` with `RecoveryController` authority; `settlement_kind = 'Interrupted'`.
- Pending reconciliation escalates (no blind replay).
- `pnpm check`: 54 files, 235 tests.

## Deviations

None.

## Commit

Recorded after commit.
