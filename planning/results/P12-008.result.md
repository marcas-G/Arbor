# P12-008 Result Record — Runtime Safety Envelope six-dimension completion

**Phase:** P12 · **Contract:** `docs/design/implementation/P12/08-runtime-safety-completion.md` (§1–§7A) · **Task:** `planning/tasks/P12-008.md` · **Depends on:** P11-013 · **Date:** 2026-09-22

## Status

**COMPLETE.** All six DID §8.16A dimensions D1–D6 implemented with the eight evidence
facets; `RuntimeSafetyPolicy` + `RuntimeSafetyGateLive(policy)`; the additive
`RuntimeSafetyObservation` channel (TR-10) widening the frozen P2 `admitActivity`;
the gate wired into the real driver/composition at ProviderTurn / ToolInvocation /
Specialist boundaries. EC-9 / blocker B3 mechanically evidenced by
`tests/p12-runtime-safety.test.ts` (20 tests). `pnpm check` green (0 lint errors).
No open Design Gap; no `docs/design/**` change.

## Files changed

Ports (P12 `08` §7A — inherited P2 evolution, TR-10):
- `packages/ports/src/execution.ts` — additive `RuntimeSafetyObservation`
  (`retryCount?` / `durableProgress?` / `chainDepth?` / `inFlight?` /
  `leaseGeneration?` / `observedAt?`); `RuntimeSafetyGateService.admitActivity`
  gains the optional third argument. The two-argument call stays valid and
  semantically unchanged.

Execution runtime (the gate):
- `packages/execution-runtime/src/runtime-safety.ts` — `RuntimeSafetyPolicy`
  (7 fields) + finite `DEFAULT_RUNTIME_SAFETY_POLICY`; `RuntimeSafetyGateLive(policy)`;
  per-Execution in-process state implementing D1–D6 (see facet table below).

Agent runtime (observation wiring):
- `packages/agent-runtime/src/driver.ts` — reports the observation channel at the
  ProviderTurn boundary (`retryCount`, `chainDepth`, `durableProgress`, `inFlight`
  begin/end + `leaseGeneration`, `observedAt`), the provider-call completion
  (`inFlight: "end"`), and each `ToolInvocation` / `SpecialistAction` boundary
  (`chainDepth`). `Stop` → `Interrupted(ControlledInterruption{reason:"RuntimeSafetyStop"})`.
  Durable progress is reported by the driver (which holds the journal capability);
  the gate never reads durable state.

Composition:
- `apps/single-workspace/src/composition.ts` — `SliceConfig.runtimeSafetyPolicy?`;
  `RuntimeSafetyGateLive(config.runtimeSafetyPolicy)`.

Test:
- `tests/p12-runtime-safety.test.ts` (new) — 20 tests (see evidence).
- `planning/results/P12-008.result.md` — this record.

## Eight evidence facets (CI-4)

| Dim | Observation source | State semantics | Reset semantics | Evaluation rule | Policy | Violation | Restart / durability | Tests |
|---|---|---|---|---|---|---|---|---|
| D1 | `RuntimeSafetyObservation.retryCount` (ProviderTurn/ProviderAttempt boundary) | per-Execution current-op retry ordinal | `retryCount === 0` (new op) | `retry >= maxRetries` | `maxRetries` | `Stop` → `Interrupted(RuntimeSafetyStop)` | in-process; reset on restart | D1 ×3 |
| D2 | `ExecutionActivity.fingerprint` | per-fingerprint count | `durableProgress: true` clears; a new fingerprint starts at 1 | `count > maxRepeatedFingerprints` | `maxRepeatedFingerprints` | same | in-process; reset on restart | D2 |
| D3 | `chainDepth` (ToolInvocation / SpecialistAction) | current chain depth | `chainDepth === 0` (new chain / tool return) | `depth > maxRecursionDepth` | `maxRecursionDepth` | same | in-process; reset on restart | D3 ×2 |
| D4 | `durableProgress` at a ProviderTurn boundary | consecutive no-progress turn counter | `durableProgress: true` → 0 | `turns > maxNoProgressTurns` | `maxNoProgressTurns` | same | in-process counter; evidence durable, counter not | D4 ×2 |
| D5 | `inFlight` + `leaseGeneration` | in-flight gauge keyed by `(executionId, leaseGeneration)` | call completion (`end`); a new lease generation discards the old gauge | `inFlight > concurrencyCeiling` | `concurrencyCeiling` | same | in-process; reset on restart | D5 ×2 |
| D6 | `observedAt` | sliding window of timestamps (epoch ms) | window expiry (timestamps older than `rateWindowMs`) | `window.length > rateLimit` | `rateLimit`, `rateWindowMs` | same | in-process; reset on restart | D6 |

All thresholds are supplied by `RuntimeSafetyPolicy`; the composition fallback is
finite on every dimension ("no silent unlimited"). The direct gate tests narrow
exactly one dimension at a time, evidencing state → reset → rule → policy →
violation.

## Acceptance evidence (EC-9 / B3)

`tests/p12-runtime-safety.test.ts` — 20/20 PASS:

Direct gate (mechanism / state / reset / rule / policy):
1. D1 `retryCount >= maxRetries -> Stop; {maxRetries:1} stops on the second attempt`
   — `[Continue, Stop]` (`retryCount 0` → C, `retryCount 1` → S).
2. D1 `policy injection: {maxRetries:3} does not stop through three attempts` — `[C,C,C]`.
3. D1 `reset on new operation (retryCount 0)` — `[C, S, C]`.
4. D2 `count > max -> Stop; reset on durable progress` — `[C,C,S,C,C]`.
5. D3 `depth > max -> Stop; reset on a new chain (chainDepth 0)` — `[C,C,S,C,C]`.
6. D3 `a SpecialistAction boundary observes the same chain depth` — `[Stop]`.
7. D4 `turns > max -> Stop; durableProgress:true resets the counter` — `[C,C,S,C,C]`.
8. D4 `an absent durableProgress signal at a turn boundary counts as no progress` — `[Stop]`.
9. D5 `in-flight > ceiling -> Stop; reset on call completion` — `[C,C,S,C,S]`.
10. D5 `gauge is lease-scoped: a stale leaseGeneration does not count against the live lease` — `[C,S,C]`.
11. D6 `N calls inside rateWindowMs Continue, (N+1)-th Stop; window expiry resets` — `[C,C,S,C,C,S]`.
12. restart `counters are in-process and reset on reconstruction (no durable upgrade)` —
    drive to `max-1` (2 calls C), reconstruct, then `[C,C,C,S]` (fresh counter; would be
    `[C,S,...]` if persisted).
13. policy surface `every dimension is configured from RuntimeSafetyPolicy (no unlimited default)`.

End-to-end (real driver + composition, per dimension):
14–19. D1–D6 `a violation interrupts with RuntimeSafetyStop and Work remains Open` —
    each builds the slice with one dimension narrowed (`maxRetries:0`,
    `maxRepeatedFingerprints:0`, `maxRecursionDepth:0` + a tool turn,
    `maxNoProgressTurns:0`, `concurrencyCeiling:0`, `rateLimit:0`), runs the real
    `CreateProject → AssignWork → ownership → select → admit → runExecution`, and
    asserts: settlement `Interrupted` / reason `RuntimeSafetyStop`, persisted
    `settlement_kind = Interrupted`, `works.lifecycle = Open` (never auto-Cancelled),
    and the `ExecutionSettled` event payload contains `RuntimeSafetyStop`.
20. `the RuntimeSafetyStop settlement is derived as an Attention fact (Attention emitted)` —
    `loadAttentionFacts` maps the settlement to a `safetyStopSettlements` fact.

## Verification (exact results)

```text
$ source ./env.sh && arbor pnpm test p12-runtime-safety
 ✓ tests/p12-runtime-safety.test.ts (20 tests) 708ms
 Test Files  1 passed (1)
      Tests  20 passed (20)
P12_TEST_EXIT=0

$ arbor pnpm typecheck
$ tsc -b && tsc -p tsconfig.test.json
TC_EXIT=0

$ arbor pnpm check
Checked 457 files in 666ms. No fixes applied.
Found 287 warnings.                       (biome: 0 errors)
 Test Files  12 passed (12)   (architecture)
      Tests  73 passed (73)
 Test Files  188 passed (188) (full suite)
      Tests  992 passed (992)
CHECK_EXIT=0
```

## Deviations / notes

1. **D1 observation is reported at the ProviderTurn boundary.** The frozen P3
   `ProviderRunInput` exposes no ProviderAttempt hook, and `ProviderRuntimeLive`
   owns transient transport retries internally (DID §6A.9). The driver therefore
   reports `retryCount` at each ProviderTurn boundary (each turn is a new provider
   operation at the driver level). Adding a ProviderAttempt callback would be an
   undeclared P3 port change; the task scopes integration wiring to the
   ProviderTurn / ToolInvocation / Specialist boundaries. The D1 mechanism is fully
   exercised through the declared observation channel (tests 1–3, 14).
2. **D3 depth convention.** A ProviderTurn reports `chainDepth: 0` (the chain root);
   a ToolInvocation / SpecialistAction reports `chainDepth: 1`. `chainDepth: 0`
   resets the chain. This makes D3 observable and trippable through the real driver
   with a single top-level tool call.
3. **`observedAt` uses the wall clock** (`new Date().toISOString()`) rather than the
   `Clock` port, to avoid widening the driver's `R` channel and cascading layer
   composition changes. D6 determinism is preserved by the direct gate tests, which
   pass explicit `observedAt` timestamps.
4. **D5 begin/end brackets the provider call.** Tool/specialist boundaries report
   D3 chain depth (one admission each); the D5 in-flight gauge is bracketed
   (begin/end) at the provider call. The D5 mechanism (including lease scoping and
   call-completion reset) is directly evidenced (tests 9–10).
5. **Durable progress definition.** The driver sets `durableProgress` from a
   journal-recorded action settlement since the previous turn boundary (P12 `08`
   §5). The gate only counts the boolean (`R = never`); the D4 counter is
   in-process (RG-11 / P9 `03` §4).
6. **TR-10 filed as an inherited P2 evolution.** The only P2 gate port change is the
   additive optional observation argument; the two-argument call is unchanged and
   P2 gating ownership is not reopened. No `docs/design/**` file was modified.
7. **No `Clock` dependency added** to `@arbor/agent-runtime`; no new package edges;
   package-DAG and P7/P8/P9 manifest-freeze architecture tests remain green.

## STOP conditions

None. No frozen-contract semantic conflict and no Design Gap: the change is exactly
the P12-owned closure (`08` §1–§7A) plus the declared TR-10 port widening and
additive observation wiring; `docs/design/**` was not modified.

## Commit

**Not committed** (per task instruction). Suggested message:

```text
feat(p12-008): complete §8.16A Runtime Safety Envelope (D1–D6), RuntimeSafetyPolicy + observation channel (TR-10), driver/composition wiring
```
