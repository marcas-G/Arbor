# P1-010 Result — CreateProject Handler

## Status

COMPLETE — `pnpm check` green.

## Source Contract

- `docs/design/implementation/P1/01-command-contracts.md` §2A, §5
- `docs/design/implementation/P1/04-sqlite-schema.md` §3.1
- DID v1.6 §12.11 (CreateProject bootstrap)

## Files

- `packages/application/src/commands/create-project.ts` — `CreateProjectPayload`,
  `CreateProjectResult`, `makeCreateProjectHandler`.
- `packages/application/src/commands/registry.ts` —
  `makeP1CommandHandlers`, `P1CommandHandlerRegistryLive`.
- `packages/application/src/gateway.ts` — `CommandHandlerError` (repository
  errors joined with store/journal errors).
- `tests/p1-create-project.test.ts`.

## Acceptance Evidence

- Project + Root Workspace + WorkspacePrimary Session created atomically
  (deferred FKs validate at COMMIT); one row each.
- Events exactly `ProjectCreated` then `WorkspaceCreated` (sequences 1, 2).
- `resourceBoundary.basisResponsibilityRevision != responsibilityRevision` →
  `AuthorityDenied` terminal rejection; no project row.
- Authority exact-match (`CreateProjectAuthority`) enforced by the gateway;
  replay of the same logical request returns the existing `Committed` receipt.
- `pnpm check`: 34 files, 174 tests.

## Notes

- P1-011/012 extend `makeP1CommandHandlers` with their handlers.

## Deviations

None.

## Design Gaps Discovered

None.

## Commit

Recorded after commit.
