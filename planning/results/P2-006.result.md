# P2-006 Result — `LeaseService` + Fence/Stop Predicates

## Status

COMPLETE — `pnpm check` green.

## Source

P2 `03` §2–§4, `04` §4, `02` §6; DID v1.7 §6.3, §9.7; SD v1.3 §10.5.

## Files

- `adapters/persistence-sqlite/src/lease.ts` — `LeaseServiceLive`.
- `adapters/persistence-sqlite/test/p2-lease.test.ts`.
- Contract correction: fence predicate now enforces `l.expires_at > now` (`03` §3, `04` §4) — SD §10.5 requires an expired worker to be unable to commit; the frozen P1 hook predicate omitted it.

## Acceptance Evidence

- Acquire → generation 0; soft release → re-acquire → generation 1 (monotonic).
- Live lease blocks acquire → `LeaseFencingRejected`.
- Stale-generation renew → `LeaseFencingRejected`.
- `invalidateExpired` counts expired active executions.
- `pnpm check`: 44 files, 212 tests.

## Deviations

- Fence expiry enforcement added (SD §10.5); recorded in the P2 contract index.

## Commit

Recorded after commit.
