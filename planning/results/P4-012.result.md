# P4-012 Result — `ToolRuntimePort` Pipeline

## Status

COMPLETE — `pnpm check` green.

## Source

P4 `02` (all); SD v1.3 §6.5/§6.6.

## Files

- `packages/tool-runtime/src/runtime.ts` — `ToolRuntimeLive`, `ToolExecutor`.
- `packages/tool-runtime/src/validation.ts` — `validateToolInput`.
- `packages/ports/src/tool.ts` — `ToolIntent.approvalId`.
- `packages/tool-runtime/test/p4-pipeline.test.ts`.

## Acceptance Evidence

- Frozen pipeline order: validation → resolution → authority → approval → admission → sandbox → execute → settle → bounded observation.
- Invalid input → `ExpectedFailure`; unknown tool / admission denial / missing approval → `Denied`.
- Intent persisted before the effect; sandbox closed via `ensuring`.
- `pnpm check`: 87 files, 323 tests.

## Deviations

- `ToolIntent.approvalId` added (needed for Exact-Intent Approval consumption).

## Commit

Recorded after commit.
