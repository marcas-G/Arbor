# P3-006 Result — Context Layers / Retention / Budget

## Status

COMPLETE — `pnpm check` green.

## Source

P3 `02` §5; DID v1.7 §8.8–§8.10.

## Files

- `packages/model-context/src/context.ts` — `ContextLayer` C0–C6, `ContextFragment`, `ContextBudget`, `contextBudget`, `planContext`, `ContextUnsatisfiable`.
- `packages/model-context/test/p3-context.test.ts`.

## Acceptance Evidence

- `B_ctx = B_model − B_output − B_protocol − B_tools` (output reserved first).
- Pinned/Protected retained; optional evicted by retention rank then layer.
- Pinned+Protected overflow → `ContextUnsatisfiable`, never silent truncation.
- `pnpm check`: 62 files, 260 tests.

## Deviations

None.

## Commit

Recorded after commit.
