# P1-014 Result — Recovery Matrix Tests

## Status

COMPLETE — `pnpm check` green.

## Source Contract

- `docs/design/implementation/P1/06-recovery-matrix.md`
- DID v1.6 §7.4, §12.5, §12.6

## Files

- `tests/support/p1-app.ts` — `faultingTransaction` (`fail-on-begin`,
  `rollback-after-body`, configurable injection call).
- `tests/p1-recovery.test.ts`.

## Matrix Coverage

| Row | Test |
|---|---|
| C2/C3/C4/C8 | `rollback-after-body` → no partial state (projects/commands/events), retryable attempt in a separate scope |
| C1/C2/C9 | `fail-on-begin` → no resolution, retryable attempt recorded |
| C5/C10/C11 | durability asserted: `PRAGMA synchronous = 2` (FULL), `journal_mode = wal` on a file DB |
| C6 | post-commit replay returns the existing receipt, one `commands` row |
| C7 | duplicate command yields one authoritative resolution |
| P1/P2 | apply-then-advance; re-delivery is idempotent (offset unchanged) |
| P3 | transient projection error → offset not advanced |
| P4 | poison (`eventVersion > 1`) → dead-letter + offset advanced, no stall |
| ownership | constraint failure rolls back claims + revision; moved revision → `ResourceResolutionStale` |
| migration | failing migration rolls back (`user_version` unchanged); retry applies |

- `pnpm check`: 34 files, 194 tests.

## Notes

- C5/C10/C11 use the frozen "durability-asserted, not crash-injected"
  mechanism (06 §1): `synchronous = FULL` + WAL on a file-backed DB.

## Deviations

None.

## Design Gaps Discovered

None.

## Commit

Recorded after commit.
