# P2-009 Result — `AdmitExecution` Handler

## Status

COMPLETE — `pnpm check` green.

## Source

P2 `01` §4–§5; DID v1.7 §3.4, §9.6, §12.3, G5.

## Files

- `packages/execution-runtime/src/commands/admit-execution.ts`.
- `packages/execution-runtime/src/commands/registry.ts` — `P2CommandHandlerRegistryLive`.
- `packages/application/src/gateway.ts` — `CommandHandlerError` gains `ExecutionRepositoryError`.
- `tests/p2-admit-execution.test.ts`.

## Acceptance Evidence

- WorkspaceMain admitted; `executions.binding_kind = 'workspace'`, `session_id` = snapshot.
- Second active main → `ActiveExecutionConflict`.
- ExecutionBound admitted atomically with an `ExecutionScoped` session; `binding_kind = 'execution_bound'`.
- Missing workspace → `WorkspaceNotFound`; authority mismatch → `AuthorityDenied`.
- `ExecutionAdmitted` emitted; `pnpm check`: 47 files, 222 tests.

## Deviations

None.

## Commit

Recorded after commit.
