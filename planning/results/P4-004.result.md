# P4-004 Result — Tool Catalog + Definitions

## Status

COMPLETE — `pnpm check` green.

## Source

P4 `01` §1, `08` §1–§4; DID v1.8 §7.6, G5/G6.

## Files

- `packages/tool-runtime/src/catalog.ts` — `read`/`patch`/`shell` `ToolDefinition` records with frozen input/result schemas; `ToolDefinitionStoreLive`; `ToolCatalogPortLive`.
- `packages/tool-runtime/test/p4-catalog.test.ts`.

## Acceptance Evidence

- Three definitions with non-empty exact schemas and stable hashes.
- `read`=ReadOnly, `patch`=Idempotent, `shell`=Reconcilable.
- Unknown tool → `None`; `ToolCatalogPort` returns the three refs.
- `pnpm check`: 78 files, 306 tests.

## Deviations

None.

## Commit

Recorded after commit.
