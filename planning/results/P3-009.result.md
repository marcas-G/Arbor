# P3-009 Result — Model-family Compiler + `ModelContextManifest`

## Status

COMPLETE — `pnpm check` green.

## Source

P3 `05` §5, `02` §6; DID v1.7 §8.19/§7.5.

## Files

- `packages/model-context/src/compiler.ts` — `ControlBasis`, `ModelContextPlan`, `ModelContextManifest`, `PreparedModelTurn`, `compileTurn`.
- `packages/model-context/test/p3-compiler.test.ts`.

## Acceptance Evidence

- `compiledRequestHash` stable for identical inputs.
- `A0`–`A3` instructions carried into the request; manifest records instruction revisions/hashes + `ControlBasis`.
- `pnpm check`: 65 files, 266 tests.

## Deviations

None.

## Commit

Recorded after commit.
