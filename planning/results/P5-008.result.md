# P5-008 Result — Restart Continuity

## Status

COMPLETE — `pnpm check` green.

## Source

P5 `04` §3; P2 `06` (mechanism owner); SD v1.3 §10.5.

## Files

- `apps/single-workspace/src/composition.ts` — wires `ReconciliationSourceStubLive` and exposes `ReconciliationSource`.
- `apps/single-workspace/test/p5-restart-continuity.test.ts`.

## Acceptance Evidence

- A **fresh** layer built on the same database file: `runRecovery` settles the in-flight stopped Execution `Interrupted(StopRequested)`.
- Execution 1's `Completed` settlement, the `work_waits` row, and session entries all survive the restart.
- A post-restart Execution (exe3) is admitted onto the **same** primary session; no state was lost or duplicated.
- `pnpm check`: 101 files, 346 tests.

## Deviations

None. Scope is a representative continuity proof only (P2 owns the mechanism; P9 owns hardening) per DID v1.9 G5.

## Commit

Recorded after commit.
