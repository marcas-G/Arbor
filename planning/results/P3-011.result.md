# P3-011 Result — ProviderRuntime + Fake Provider + Failure Model

## Status

COMPLETE — `pnpm check` green.

## Source

P3 `01` §1–§9; DID v1.7 §6A.8/§6A.9/§9.10/§9.11.

## Files

- `packages/ports/src/provider.ts` — `ProviderTurnStore` + `ProviderTurnRecord`/`ProviderAttemptOutcome`; re-exports the domain ID types for the ports-only edge.
- `packages/provider-runtime/src/runtime.ts` — `ProviderRuntime`/`ProviderRuntimeLive` (intent-before-request, safe retry, attempt/turn recording, usage extraction).
- `adapters/persistence-sqlite/src/provider-turns.ts` — `ProviderTurnStoreLive`.
- `adapters/provider-fake/src/index.ts` — `FakeProviderLive`.
- `adapters/persistence-sqlite/test/p3-provider.test.ts`.

## Acceptance Evidence

- Fake provider streams the frozen `CanonicalProviderEvent` ADT; turn + attempt recorded, turn settled with `finish_reason`.
- A retryable failure retries and records `RetryableFailure` then `Success`.
- Provider retry does not create a new Turn or bump `turnNo`.
- `pnpm check`: 67 files, 272 tests.

## Deviations

- `ports` re-exports the domain ID types so `provider-runtime` keeps its ports-only edge.

## Commit

Recorded after commit.
