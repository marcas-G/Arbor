# P4-013/014/015 Result — read / patch / shell tools

## Status

COMPLETE — `pnpm check` green.

## Source

P4 `08` §1–§4; DID v1.8 G5/G6.

## Files

- `packages/tool-runtime/src/tools/{read,patch,shell,index}.ts` — `readExecutor` (ReadOnly), `patchExecutor` (Idempotent), `shellExecutor` (Reconcilable), `shellPolicy`, `BUILTIN_EXECUTORS`.
- `packages/tool-runtime/test/{p4-read,p4-patch,p4-shell}.test.ts`.
- `ToolExecutor.requiresApproval` is policy-driven (function).

## Acceptance Evidence

- `read`: bounded slice; path escaping the sandbox root is rejected.
- `patch`: unified diff applied; no-hunk → `ExpectedFailure`.
- `shell`: deterministic policy (`Allow`/`RequireApproval`/`Deny`); denied command → `ExpectedFailure`; allowed command runs in the sandbox.
- `pnpm check`: 90 files, 330 tests.

## Deviations

- `ToolExecutor.requiresApproval` became a predicate so `shell` can require approval only for destructive commands.

## Commit

Recorded after commit.
