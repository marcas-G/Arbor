# P1-013 Result — Idempotency / Replay / Concurrent-Duplicate Tests

## Status

COMPLETE — `pnpm check` green.

## Source Contract

- `docs/design/implementation/P1/01-command-contracts.md` §8
- `docs/design/implementation/P1/03-transaction-model.md` §3.1, §3.4
- DID v1.6 §4.1, §12.5

## Files

- `tests/p1-idempotency.test.ts` — end-to-end integration tests using the real
  SQLite adapter (`CommandStoreLive`, `DomainEventJournalLive`,
  `TransactionPortLive`) and the real `CommandGatewayLive`.

## Acceptance Evidence

- Same id + same fingerprint → existing `Committed` receipt; handler executed
  once; exactly one `commands` row.
- Same id + same fingerprint after a terminal rejection → existing
  `TerminalRejected` receipt; handler executed once.
- Same id + different fingerprint → `IdempotencyConflict`; existing
  `result_json` unchanged; handler not re-executed.
- Concurrent duplicates (two executions, concurrency 2, shared file DB) →
  exactly one authoritative `commands` row; both receipts carry the same
  fingerprint (loser re-reads the committed resolution).
- Operational failure (`TransactionOperationalFailure`) → no `commands` row, a
  `RetryableOperationalFailure` attempt (attempt 0) in a separate transaction;
  retry commits as attempt 1.
- `pnpm check`: 34 files, 162 tests.

## Notes

- The integration test imports sibling packages by relative source path because
  the root workspace has no `@arbor/*` links; adding root devDependencies was
  rejected by the pinned offline package mirror (`pnpm install` unavailable).
  This keeps `packages/*/package.json` edges unchanged (architecture green).

## Deviations

None.

## Design Gaps Discovered

None.

## Commit

Recorded after commit.
