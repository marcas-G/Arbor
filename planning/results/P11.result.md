# P11 Result Record — Environment / Git / Advanced Sandbox

**Phase:** P11 · **Contracts:** `docs/design/implementation/P11/**` (FROZEN; GQ1–GQ5 + scope fence; CI review Blocking=0, CI-1..CI-5 all PASS) · **Plan:** `planning/phases/P11.md` (13 tasks + session-split P11-002A) · **Date:** 2026-09-22

## Verdict

**COMPLETE — 11/11 exit criteria PASS; CI-1..CI-5 mechanically proven; no open Design Gap.**

## Exit criteria matrix (`planning/phases/P11.md`)

| # | Criterion | Evidence |
|---|---|---|
| 1 | `pnpm check` green | 185 files / 959 tests / 0 errors |
| 2 | CI-1..CI-5 each backed by mechanical tests | p11-closure (5) + p11-acceptance (7): single advancement path (observe 100× → counter immobile); real ControlBasis (hardcode gone, DecisionStale live); worktree terminal closure (release-first enforced); drift convergence (NoDrift ∥ governed REC ∥ Attention-stop); verdict immutability (four-state overlay, structuredClone bit-equal) |
| 3 | Revision algebra enforced | p11-revision-algebra (14): fingerprint ordering banned structurally; ABA advances counter; parse validation |
| 4 | REC: CAS + one-reprobe retry + Attention escalation; event + same-transaction wake | p11-record-change (14) |
| 5 | Real resolver probes FileTree/GitWorktree; stale genuinely triggerable + bounded re-resolve | p11-resolver (7) + p11-ownership-wiring (7) |
| 6 | Drift: explicit snapshot-diff; startup seam; auto-submit default off | p11-drift (13) |
| 7 | Impact: broad wake + narrow overlay; resolved-region inputs | p11-impact (8) |
| 8 | Verdict immutability: overlay derived; PASS never auto-mutates; Attention row | p11-staleness (7) + p11-verdict-consumer (6) |
| 9 | ControlBasis reads store inside service; worktree lifecycle + §1.4A-enforcing retire; ownership claim/release real call sites | p11-controlbasis (4) + p11-worktree (6) + p11-ownership-wiring (7) |
| 10 | Sandbox handoff: worktree-backed adapter + env allow-list repaid; four guarantees | p11-sandbox-handoff (9) |
| 11 | No open P11 Design Gap; P5–P10 regressions green | full check green; two P12 convergence items recorded below |

## Task completion

| Task | Status | Suite |
|---|---|---|
| P11-001 revision algebra + snapshot domain | ✅ | p11-revision-algebra (14) |
| P11-002A snapshot/fingerprint identity (session split) | ✅ | p11-snapshot-fingerprint (12) |
| P11-002 RecordEnvironmentChange | ✅ | p11-record-change (14) |
| P11-003 real resolver | ✅ | p11-resolver (7) |
| P11-004 drift detection + startup seam | ✅ | p11-drift (13) |
| P11-005 impact evaluation | ✅ | p11-impact (8) |
| P11-006 staleness overlay | ✅ | p11-staleness (7) |
| P11-007 ControlBasis binding | ✅ | p11-controlbasis (4) |
| P11-008 worktree lifecycle | ✅ | p11-worktree (6) |
| P11-009 ownership wiring (fenced) | ✅ | p11-ownership-wiring (7) |
| P11-010 sandbox handoff | ✅ | p11-sandbox-handoff (9) |
| P11-011 verdict-consumer integration | ✅ | p11-verdict-consumer (6) |
| P11-012 acceptance + CI proofs | ✅ | p11-acceptance (7), p11-closure (5) |
| P11-013 convergence & result record | ✅ | this file |

## GQ fidelity

- **GQ1(a)** worktree lifecycle delivered as commands/events/state with the §1.4A precondition **enforced** (typed rejection points at release paths; never bypassed) — B1 fix held.
- **GQ1(b)** minimal ownership wiring inside the verbatim scope fence (mechanically asserted: no lease/TTL/preemption/multi-owner/distributed-lock/arbitration).
- **GQ2** drift v1 = explicit snapshot-diff; startup probe seam (before runnable rebuild); watcher excluded.
- **GQ3'** counter ∥ fingerprint fully separated; anchor advances only via REC (CI-1); lazy-init per P1-DG-08 (strict no-op hardened to DO NOTHING); CAS with one-reprobe + Attention escalation; ABA advances.
- **GQ4** Verdict×Freshness derived overlay; forbidden transitions unreachable; ControlBasis reads the store inside the service (hardcode removed).
- **GQ5** wake broad / invalidate narrow — no region revision algebra anywhere.

## Deviations & P12 convergence items

1. **advanceAnchor residual exposure**: `EnvironmentRevisionStore.advanceAnchor` remains on the ports public surface (documented in p11-closure as a P12 TODO); application-layer discipline + DI boundaries enforce CI-1 today, and every observation-side source is mechanically proven free of advancement imports.
2. **Region encoding seam**: the resolver produces string `normalizedRegion` values while the frozen `resourceRegionComparator` uses the object encoding — the narrow overlap semantics hold via the frozen comparator's encoding (acceptance Story A proves STALE correctly), but the representation should converge in P12.
3. **parseSnapshotBlob mtime roundtrip**: the delimiter-based blob encoding cannot roundtrip ISO mtimes containing colons; production RegionDiff degrades to the conservative full-set mode (safe direction). Blob format hardening is a P12 convergence item.
4. **EnvironmentChanged event persistence** in REC is inlined (journal-sequence SQL) rather than via the journal port — same transaction, same table, same allocation rule; recorded as an implementation choice (port R-type friction).
5. **Session task split**: P11-002A (snapshot identity) recorded in the phase plan; mapping pinned to the frozen contract sequence.

## Verification

```bash
pnpm check                # 959 tests green
pnpm test p11-acceptance  # Stories + CI proofs (7)
pnpm test p11-closure     # CI evidence aggregation (5)
pnpm architecture         # 73 architecture tests
```
