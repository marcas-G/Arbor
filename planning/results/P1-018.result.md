# P1-018 Result — Domain artifact evolution

## Status

COMPLETE — `pnpm check` green.

## Source Contract

- `docs/design/implementation/P1/01-command-contracts.md` §2, §4
- `docs/design/implementation/P1/03-transaction-model.md` §7
- DID v1.6 §6A.15, §4.1

## Files

- `packages/domain/src/command.ts`: `CommandResolution<Result, Rejection>` and
  `CommandReceipt<Result, Rejection>` parameterized; `CommandReceipt` evolved to
  the frozen 8-field view (`semanticRequestFingerprint` rename + projectId,
  schemaVersion, fingerprintAlgorithmVersion, createdAt, settledAt); removed the
  interim FNV-1a `semanticRequestFingerprint` and `canonicalize`.
- `packages/domain/test/command.test.ts`, `invariants.test.ts` updated.
- `packages/ports/test/command-receipt.test.ts`: ports consumes
  `CommandReceipt<unknown, unknown>` (no `ports → application` edge).
- Root `package.json` `typecheck` = `tsc -b && tsc -p tsconfig.test.json`
  (cross-package test types resolve against freshly built `dist`).

## Acceptance Evidence

- `packages/domain` compiles with the parameterized types + frozen receipt view.
- P0 command/invariant tests updated and green.
- `ports` imports `CommandReceipt<unknown, unknown>` without application.
- `pnpm check`: 22 files, 127 tests.

## Deviations

1. `typecheck` now builds (`tsc -b`) before typechecking tests, so cross-package
   test imports resolve to current `dist` (harness fix; P1-017 scope).

## Design Gaps Discovered

None.

## Commit

Recorded after commit.
