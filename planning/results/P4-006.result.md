# P4-006 Result — Trusted `InvocationAuthority` + Capability Ceiling

## Status

COMPLETE — `pnpm check` green.

## Source

P4 `03` §1–§3; DID v1.8 G1; SD v1.3 §8.1–§8.3.

## Files

- `packages/tool-runtime/src/authority.ts` — `checkInvocationAuthority`.
- `packages/ports/src/tool.ts` — `InvocationAuthority.allowedCapabilities`; `ToolExecutionContext.authenticatedPrincipal`.
- `packages/tool-runtime/test/p4-authority.test.ts`.

## Acceptance Evidence

- Exact match → `Ok`; every mismatch (principal/workspace/execution/tool/control basis/expiry/delegation depth/resource space/capability) → `Denied`.
- No PermissionGrant/Parent/User resolution; no default-allow.
- `pnpm check`: 80 files, 310 tests.

## Deviations

- Added `allowedCapabilities` + `authenticatedPrincipal` to the P4 ports (needed for exact-match).

## Commit

Recorded after commit.
