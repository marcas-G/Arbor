# P1-012 Result — AssignWork Handler

## Status

COMPLETE — `pnpm check` green.

## Source Contract

- `docs/design/implementation/P1/01-command-contracts.md` §2A, §7
- `docs/design/implementation/P1/04-sqlite-schema.md` §3.2
- DID v1.6 §0A.1, §6A.15, §12.10, §12.11 Work

## Files

- `packages/application/src/commands/assign-work.ts` —
  `AssignWorkPayload`, `AssignWorkResult`, `makeAssignWorkHandler`.
- `packages/application/src/commands/registry.ts` — registers all three P1
  handlers; layer now requires `WorkRepository`.
- `docs/design/implementation/P1/01-command-contracts.md` §7 — note that
  "work outside responsibility scope" is expressed by the trusted authority
  fact (§2A); P1 does not evaluate the responsibility definition.
- `tests/support/p1-app.ts` — added `seedProject`, `eventTypes`.
- `tests/p1-assign-work.test.ts`.

## Acceptance Evidence

- Work created `Open` bound to the workspace; `WorkAssigned` emitted.
- Cross-project workspace → `AuthorityDenied`.
- Closed project → `TerminalLifecycleMutation` (entity `Project`).
- Retired workspace → `TerminalLifecycleMutation` (entity `Workspace`).
- Stale workspace revision → `RevisionConflict`.
- Authority exact-match (`AssignWorkAuthority` + `targetWorkspaceId`) enforced
  by the gateway; replay returns the existing receipt.
- `pnpm check`: 34 files, 184 tests.

## Deviations

None.

## Design Gaps Discovered

None. (The §7 scope row is covered by the P1-DG-11 authority fact; recorded as
a contract clarification, not a new gap.)

## Commit

Recorded after commit.
