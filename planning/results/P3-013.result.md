# P3-013 Result — Real `ExecutionDriverPort` + Control Loop + Safety Gating

## Status

COMPLETE — `pnpm check` green.

## Source

P3 `03` §1–§2/§5–§6; DID v1.7 §10.6, G4.

## Files

- `packages/agent-runtime/src/driver.ts` — `AgentDriverLive` implementing the P2 `ExecutionDriverPort`.
- `packages/ports/src/provider.ts` — `ProviderRuntime` moved to a ports service (so `agent-runtime !-> provider-runtime` holds).
- `packages/model-context/src/decode.ts` — reserved `arbor_directive` structured directive decoding.
- `adapters/provider-fake/src/index.ts` — per-turn scripts.
- `tests/p3-driver.test.ts`.

## Acceptance Evidence

- Multi-turn loop: text turn → directive turn → `Completed(CompletionClaimed)`.
- Safety `Stop` at the P2 gate → `Interrupted(RuntimeSafetyStop)`; Work stays Open.
- The driver returns a settlement proposal; it never writes canonical state.
- `pnpm check`: 69 files, 279 tests.

## Deviations

- `ProviderRuntime` is a `ports` service (DAG: `agent-runtime !-> provider-runtime`).
- Organizational directives are decoded from a reserved structured tool call (`arbor_directive`); provider events never carry an `AgentDirective`.

## Commit

Recorded after commit.
