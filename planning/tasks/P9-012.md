# P9-012 — P7/P8 workflow interruption replay + dispatch failure (WF1–WF3 / DF1–DF2 / D5)

## Source

- P9 `02` §8 (WF1–WF3), §9 (DF1–DF2, no-ack-surface note), §4 (D5); `04` §4 (dispatch failure semantics systematized); P7 `04` §2/§4; P8 `02` §1/§3; P2 `02` §5, P2 `05` §8

## Depends On

- P9-001, P9-003, P9-010

## Objective

Workflow-level interruption replay for the P7 coordinator and the P8 verifier
chain (consumers wired onto offset infra in P9-010), plus dispatch-failure /
lost-dispatch injection over the minimal observable surface.

## Outputs

- `p9-workflow-replay` suite.

## Must Hold

- **WF1**: coordinator consumption interrupted (event delivered, SatisfyDependency command not committed) → redelivery → deterministic CommandId → convergence to identical canonical state.
- **WF2**: verifier spawn window (StartVerification committed, spawn/backfill not yet) — the window is closed by design (caller-preallocated executionId, P8 `02` §1); **P9 injects to verify the closure holds**: replay re-spawns with the same caller-preallocated executionId; AdmitExecution idempotent; no permanent "committed without Verifier" state; no P8 spawn-window reopening.
- **WF3**: consumer A / consumer B interrupted anywhere → replay converges (deterministic ids; partial unique indexes; idempotent admission); no lost wake, no duplicate Verification/Completion.
- **DF1**: dispatch port call fails → **never settles an Execution** (P2 `02` §5); Execution stays Active; scheduler reevaluation re-dispatches.
- **DF2**: dispatch accepted then lost (wake intent lost / worker never starts, incl. W1) → bottom line = next sweep reevaluation (T2/T3) re-dispatches; single-flight conflict converges to Noop (P2 `05` §8); bounded takeover latency = sweep interval (empirical).
- **No dispatch ack surface; no new durable dispatch record** (GQ ruling: no dispatch table) — the observable surface is Execution state only: Active + no live lease + no session progress → re-dispatch within N sweeps.
- **D5**: Open verification in the dirty-restart fixture remains Open; no auto-conclude, no auto-Unknown (orphan re-drive is an explicit P8 governance action, P8 `02` §3).

## Must Not Decide

- No P7/P8 consumer semantics changes; no dispatch table / ack surface; no spawn-window redesign; no scheduler decision changes.

## Acceptance

- WF1–WF3 / DF1–DF2 / D5 mechanically checked (crash-injected class); convergence asserted as identical canonical state (no duplicate canonical effects, no lost wake).

## Verification

```bash
pnpm test p9-workflow-replay
pnpm check
```
