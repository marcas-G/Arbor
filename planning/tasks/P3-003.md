# P3-003 — P3 DDL migrations

## Source

- P3 `04` (all)

## Depends On

- P3-002

## Objective

Add migration `0003_provider_model_context`.

## Outputs

- `provider_turns`, `provider_attempts`, `model_context_manifests`.
- `P3_MIGRATIONS` with `user_version = 3`.

## Must Hold

- Turn intent + manifest persisted before the request.
- `attempt_no` Turn-local; `settled_at`/`finish_reason` consistency CHECK.
- Forward-only additive migration.

## Must Not Decide

- No P4/P8 tables.
- No modification of 0001/0002.

## Acceptance

- Fresh DB migrates to 3.
- CHECKs reject inconsistent rows.

## Verification

```bash
pnpm test p3-ddl
pnpm check
```
