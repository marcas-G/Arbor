# P3-002 — `ports` P3 contracts

## Source

- P3 `01` §1–§9, `02` §1/§7, `06` §1

## Depends On

- P3-001

## Objective

Declare the P3 Effect service contracts and port errors.

## Outputs

- `ProviderPort` (+`PortableModelRequest`, `ProviderExecutionContext`, `CanonicalProviderEvent`).
- `ModelCapabilityPort`, `SkillRegistry`, `AgentContextSourcePort`, `KnowledgeQueryPort`, `SecretStorePort`.
- `ToolCatalogPort` **contract** (minimal; P4 owns tool definitions/implementation) so `model-context` `R` compiles without a P4 edge.
- P3 port errors (`ProviderFailure`, `ModelCapabilityError`, `SkillRegistryError`, `ModelContextError`).

## Must Hold

- No universal error; each port narrow E.
- `CanonicalProviderEvent` never carries an `AgentDirective`.
- Port R exposes no SQLite/SDK.

## Must Not Decide

- No adapter imports in ports.
- No behavior.

## Acceptance

- A/E/R match the contracts; typecheck green.

## Verification

```bash
pnpm test p3-ports
pnpm check
```
