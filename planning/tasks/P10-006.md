# P10-006 — Workspace Detail / Verification / Dependency / CurrentWork views

## Source

- P10 `01` §1 (Workspace Detail ①–⑥, Verification, Dependency, Current Work rows + derive inputs); P10 `05` §1 (WorkspaceDetailReq / CurrentWorkReq / VerificationReq / DependencyReq cores); P10 `06` §3 (audit timeline renders intervention facts in order); SD §12.4 ①–⑥

## Depends On

- P10-003

## Objective

The four on-read views over canonical tables + journal, per the frozen
DTO cores: Workspace Detail (incl. the audit timeline as the ⑥
substitute), Current Work, Verification, Dependency.

## Outputs

- Four on-read view derives + ProjectionQueryPort bindings; audit-timeline renderer; `p10-views` suite.

## Must Hold

- `WorkspaceDetailReq → {responsibility, boundary, currentWork?, pendingWorks[], executionSummary?, dependencies[], inboxUnconsumed[], verification?, auditTimeline[]}` — derive inputs per `01` §1 (workspaces / works / executions / dependencies+messages+inbox / verification+evidence / journal), read-only.
- **⑥ = audit timeline from domain_events** — renders `HumanInterventionApplied` (and other domain events) in order; the **Memory/Decisions store is NOT built** (deferred; audit timeline substitutes — boundary list in `00`).
- CurrentWork / Verification / Dependency shapes per `05` §1 cores: CurrentWork `{workId?, objective, status, activeExecution?} | null`; Verification `{verificationId?, verdict?, criteriaResults[], evidenceRefs[], acceptance?}`; Dependency `rows[{dependencyId, consumerWorkId, binding, state, satisfiedBy?}]` with `projectId | workspaceId` scoping.
- Status references reuse the P10-003 label map — no label re-derivation.
- No view writes anything (write-count snapshot).

## Must Not Decide

- No Memory/Decisions store; no Search; no transport; no derive inputs beyond the `01` frozen list; no new fields beyond the frozen cores (rendering details task-level).

## Acceptance

- Per-view fixtures → derive assertions against the frozen cores; audit timeline ordering test (journal-sequenced, intervention facts included); no-write snapshots green.

## Verification

```bash
pnpm test p10-views
pnpm check
```
