# P12-009 — Region-encoding convergence + P11 suite migration

## Source

- docs/design/implementation/P12/09-region-encoding-convergence.md §1–§7

## Depends On

- P12-005

## Objective

Resolver emits frozen object encoding (`filesystem` + `{kind,...}`); `canonicalRegionString`; all-producer assertion; P11 suite/fixture migration (p11-resolver/p11-drift/p11-snapshot-fingerprint/p11-record-change + Story-A re-wrap removal); production wiring; alias collapse preserved.

## Outputs

`adapters/environment-resolver-local` + `packages/application`/`domain` stringifier + migrated P11 fixtures; suite `p12-region-encoding`.

## Must Hold

- `09` §3–§7; narrow invalidation reachable end-to-end (CI-7); no region revision algebra; blob goldens re-baselined with P12-005.
- Blocker: B1

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-10 mechanically checked (see `14-acceptance.md` §3.1/§3.2) ; blocker B1 evidenced.

## Verification

```bash
pnpm test p12-region-encoding
pnpm test p11-resolver
pnpm test p11-drift
pnpm test p11-snapshot-fingerprint
pnpm test p11-record-change
pnpm test p11-closure
pnpm check
```
