# P9-013 — Acceptance stories A–G + architecture + P5–P8 regression guards

## Source

- P9 `06` 全部 (Stories A–G + 机械断言清单); `02` (matrix row coverage); `03` §4 (RuntimeSafetyGate counter-continuity note); DID v1.11 §10.4.1; P5/P6/P7/P8 suites (regression guards)

## Depends On

- P9-002, P9-003, P9-004, P9-005, P9-006, P9-007, P9-008, P9-009, P9-010, P9-011, P9-012

## Objective

The seven deterministic acceptance stories (all injection real
process/transaction/fiber level per GQ5, one durable DB, existing composition
root), the P9 architecture tests, the P5–P8 regression guards, and the
matrix-coverage audit.

## Outputs

- `tests/acceptance/p9-*.test.ts` seven stories + `tests/architecture/` P9 edges + coverage-audit table.

## Must Hold

- **Story A — 恢复可见性门** (B-1 before/after): pre-fix snapshot (the defect shape recorded); post-fix injection of the same scenario → escalate / `OutcomeUnknown` (**永不 Interrupted**) + durable Attention fact (journal assertion) + presentation zero-promise.
- **Story B — 脏重启九步一次** (D series, dual fixtures):
  - B-清洗: stop requested and **all invocations settled** (no pending side effects) → T1 → settle `Interrupted(StopRequested)` (I-2 clean path) + completion-fact fixture → corresponding Completed; lease invalidation; runnable rebuild; timers re-fired; repeated T1 idempotent.
  - B-未决: active execution + unsettled ReadOnly + Reconcilable + NonIdempotent invocations + pending wake + open verification + expired lease → T1 → four-tier dispositions (ReadOnly/Idempotent safe settle, Reconcilable reconcile, NonIdempotent → escalate / `OutcomeUnknown` **永不 Interrupted** — I-1 after-fix) + escalation lands as durable Attention facts.
- **Story C — 复活与过期** (R/L): stale worker five write surfaces all `FencingRejected`; post-expiry settle rejected; renewal boundary (TTL/3: success commits, post-expiry rejected); pre-dispatch T4 checks the fence predicate only (asserts no nine-step side effects).
- **Story D — provider disconnect** (PD): connect-phase inject → `ProviderUnavailable` retry boundary; mid-stream inject → `StreamInterrupted` retry; crash-left unsettled ProviderTurn → recovery same Turn new Attempt (**turnNo / Manifest unchanged** assertions); bound exhausted → existing Turn-failure semantics.
- **Story E — tool 四档** (T): dangling detection (intent without settlement); ReadOnly retry; Idempotent same-identity; Reconcilable reconcile→settle; NonIdempotent → `OutcomeUnknown` **永不自动 replay** (No.35).
- **Story F — consumer/重建** (CC/RB): mid-batch crash replay idempotent; offset regression absorbed by deterministic ids; two poison paths dead-letter; `rebuildProjection` floor-refusal / reset / replay idempotency (GQ2 P9 face); P10 boundary negative assertion (business projection rebuild not in the P9 assertion set).
- **Story G — P7/P8 工作流中断** (WF): coordinator consumption-interruption replay converges; verifier spawn window (closed by P8 — injection verifies no regression); consumer A/B interruption replay; all via deterministic id idempotency.
- **机械断言清单**: every `02` matrix row has ≥1 mechanical case across the p9-* suites; guarantee class labeled per row (crash-injected vs durability-asserted); durability-asserted items evidenced by reopen + integrity_check + user_version + count reconciliation (`02` §12); P5/P6/P7/P8 regression guards all green; pre-dispatch T4 runs no nine steps (side-effect count = 0).
- Dirty-restart stories must not assume RuntimeSafetyGate counter continuity across the restart boundary (`03` §4 note).
- Architecture: P9 suites/harness placement per §10.4.1; no production-code fault hooks outside the envelope discipline; consumer paths never bypass the gateway.
- All artifacts named "Phase P9 / recovery hardening" (B-10 disambiguation vs DID §8.4 P9 Program).

## Must Not Decide

- No real models/humans as gating; no P10/P11/P12 assertions; no UI acceptance (P10); no new matrix rows.

## Acceptance

- Stories A–G green in one durable-DB run; coverage-audit table complete (row → suite → guarantee class); `pnpm architecture` green; P5–P8 guards unchanged-green.

## Verification

```bash
pnpm test p9-acceptance
pnpm test p9-architecture
pnpm architecture
pnpm check
```
