# P1-015 — Architecture tests for new packages

## Source

- DID v1.6 §10.4, §10.4.1, §10.3
- `tests/architecture/package-dag.ts` (P0-017)

## Depends On

- P1-001 (adapter)
- P1-002 (`ports`)

## Objective

Extend the architecture tests to cover the P1 packages and enforce the DAG.

## Outputs

- Updated allowed-edge matrix entries for `ports`, `application`,
  `adapters/persistence-sqlite`, `adapters/environment-local`.
- Tests asserting: `domain` has no infra deps; `ports → domain` only;
  `application → domain, ports`; `adapters/* → domain, ports`;
  `runtime !-> adapters`; no deep imports; no catch-all error / service locator.

## Must Hold

- The checker stays data-driven and non-vacuous.
- `domain` remains dependency-free.
- New packages are covered automatically.

## Must Not Decide

- No package-level behavior.
- No weakening of the matrix.

## Acceptance

- `pnpm architecture` green for the P1 package set.
- Synthetic-manifest tests still fail on forbidden edges/unknown packages.
- `domain` proven dependency-free.

## Verification

```bash
pnpm architecture
pnpm check
```
