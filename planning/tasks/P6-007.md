# P6-007 — SpawnSpecialist → ExecutionBound admission

## Source

- P6 `01` §3; DID v1.7 G5; P2 `01` §5

## Depends On

- P6-001

## Objective

Implement specialist spawn: `SpecialistSpec` → `AdmitExecution { _tag: "ExecutionBound", parentExecutionId, mission, sessionId }` via `CommandGateway`.

## Outputs

- Directive handler + specialist execution bootstrap（constraints/skillIds 注入 Session 控制上下文）.

## Must Hold

- All P2 `01` §5 preconditions apply unchanged; owning workspace == 发起 Execution 的 owning workspace.
- After `StopExecution`, new spawn admission is refused（DID §3.4 quiescence）.
- No specialist concurrency limit introduced（Safety Envelope 兜底）.

## Must Not Decide

- No `AdmitExecution` semantic changes; no settlement routing (P6-008).

## Acceptance

- Spawn creates durable Execution + `ExecutionScoped` Session atomically; quiescence refusal is typed.

## Verification

```bash
pnpm test p6-specialist-spawn
pnpm check
```
