# P2-003 — `ports` P2 contracts

## Source

- P2 `02` (all), `01` §2; DID v1.7 §7.1–§7.4, §9.7

## Depends On

- P2-002

## Objective

Declare the P2 Effect service contracts and port errors in `packages/ports`.

## Outputs

- `ExecutionRepositoryService` + `LeaseRecord`.
- `SessionRepositoryService.appendEntry` + `SessionEntryKind`.
- `FenceStopCheckService` with `stopAdmission`.
- `WorkerDispatchPortService`, `ExecutionDriverPortService`, `RuntimeSafetyGateService`.
- `LeaseService`, `ExecutionSchedulerService`, `RunnableWorkSourceService`, `WorkWaitStoreService`, `SchedulerTimerStoreService`.
- P2 port errors (`ExecutionRepositoryError`, `WorkerDispatchError`, `ExecutionDriverError`, `ExecutionSchedulerError`, `WorkWaitStoreError`, `SchedulerTimerStoreError`, `RunnableWorkSourceError`, `LeaseFencingRejected`).

## Must Hold

- No universal `RepositoryError`; each port declares a narrow `E`.
- Persistence ports require `TransactionScope`; driver/dispatch/safety do not.
- No CRUD; invariant-shaped methods only.

## Must Not Decide

- No adapter imports in `ports`.
- No behavior/implementation.

## Acceptance

- A/E/R match `02`; typecheck green.
- Repository ports require `TransactionScope`; `ProjectEnvironmentPort`/`Clock`/`IdGenerator` unchanged.

## Verification

```bash
pnpm test p2-ports
pnpm check
```
