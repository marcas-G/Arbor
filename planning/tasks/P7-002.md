# P7-002 — P7 DDL + Dependency/Deliverable repositories

## Source

- P7 `01` §10 (persistence boundary), §4 (concurrency); DID §9.3, §12.10 (store column)

## Depends On

- P7-001

## Objective

Add migration id 6: `dependencies` / `deliverables` / `deliverable_artifacts` tables, plus `DependencyRepository` (state + revision CAS) and `DeliverableRepository` (immutable facts).

## Outputs

- Migration `0006_p7_dependency_deliverable.sql` + repository wiring.

## Must Hold

- `DependencyRepository` applies state + revision CAS — first committer wins (P6 `FormationProposalStore.decideIfPendingRevision` precedent).
- `deliverables` / `deliverable_artifacts` are immutable — no update path.
- Deliverable rows bind `sourceWorkId` + `sourceWorkRevision` (No.49 traceability).
- SQL shape and migration numbering are task-level, constrained by architecture tests.

## Must Not Decide

- No semantics beyond contract storage needs; no lookup-index freeze (G5).

## Acceptance

- Migration applies/reverts cleanly; repository round-trip tests pass; concurrent CAS: first committer wins, loser observes the moved revision.

## Verification

```bash
pnpm test p7-ddl
pnpm check
```
