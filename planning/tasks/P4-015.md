# P4-015 — `shell` tool + policy enforcement

## Source

- P4 `08` §4; DID v1.8 G6

## Depends On

- P4-012

## Objective

Implement the versioned `shell` tool and the frozen shell policy mechanism.

## Outputs

- `shell` executor; deterministic policy evaluation (allow/deny, path confinement, env allow-list, limits, destructive → approval).

## Must Hold

- Reconcilable semantics; reconcile before replay.
- Policy mechanism is contract; lists/numbers empirical.
- stdout/stderr above size → Artifacts.

## Must Not Decide

- No git/worktree operations (P11).
- No unbounded execution.

## Acceptance

- Denied command → Denied; destructive command requires approval; output artifacts created.

## Verification

```bash
pnpm test p4-shell
pnpm check
```
