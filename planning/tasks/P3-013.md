# P3-013 — Real `ExecutionDriverPort` + control loop + safety gating

## Source

- P3 `03` §1–§2/§5–§6; DID v1.7 §10.6, G4

## Depends On

- P3-010
- P3-011
- P3-012

## Objective

Implement the real agent control loop over the P2 driver seam.

## Outputs

- `AgentDriverLive` implementing `ExecutionDriverPort`.
- Loop: load → prepareTurn → provider → decodeTurn → execute → Observation → continue/settle.
- Safety gate calls at every activity boundary.

## Must Hold

- Driver returns a settlement proposal; P2 persists via `SettleExecution`.
- Safety `Stop` → `Interrupted(RuntimeSafetyStop)`, Work Open.
- Session entries via P2 `appendEntry` (fenced).

## Must Not Decide

- No canonical writes outside `CommandGateway`.
- No lease/settlement mechanics (P2).

## Acceptance

- Multi-turn continuity over the fake provider; settle once via the pipeline.

## Verification

```bash
pnpm test p3-driver
pnpm check
```
