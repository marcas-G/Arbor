# P3-010 — `prepareTurn` assembly

## Source

- P3 `02` §1–§2/§6; DID v1.7 §6A.10/§8.2

## Depends On

- P3-005
- P3-006
- P3-007
- P3-008
- P3-009

## Objective

Assemble the full `prepareTurn` pipeline.

## Outputs

- `prepareTurn` returning `Ready | NeedsCompaction | GovernanceBlocked`, E `ContextUnsatisfiable | ModelContextError`.
- Agent policy resolution.

## Must Hold

- Control ADT vs typed E split exact.
- `R` has no SQLite/SDK.

## Must Not Decide

- No provider call inside prepareTurn.

## Acceptance

- All three control results + `ContextUnsatisfiable` reachable and tested.

## Verification

```bash
pnpm test p3-prepare-turn
pnpm check
```
