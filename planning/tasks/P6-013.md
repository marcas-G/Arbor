# P6-013 — Prompt Programs v1 + dual versioning + eval gate (D4)

## Source

- P6 `05` §2–§5; DID §8.4/§8.4A, §13 表; AGENTS.md 规则 6

## Depends On

- P6-004, P6-006, P6-008, P6-010, P6-012

## Objective

Author the four Program v1 texts（formation / communication / bootstrap / human-steer）with mandatory clauses, implement the contractRevision/textRevision dual versioning, and the behavioral eval suites E1–E4.

## Outputs

- `promptPrograms/{formation,communication,bootstrap,human-steer}/v1.md` + eval harness + version-gate check.

## Must Hold

- Every file header carries `familyId, contractRevision, textVersion, textHash, changelog`; Manifest hash match enforced.
- **Text bump without full eval rerun is rejected（合入门）**; eval is deterministic（Fake Provider 脚本）.
- contractRevision changes are governance-only（agent 不可改契约层）.
- Mandatory clauses of `05` §2 all present（缺一即 eval 失败）.

## Must Not Decide

- No Output Contract changes (P3); no real-model tuning baselines.

## Acceptance

- E1–E4 green per program; induced-violation cases（Report 当交付、文本层抢占）fail loudly.

## Verification

```bash
pnpm test p6-prompt-programs
pnpm check
```
