# P5-001 — `apps/single-workspace` composition root

## Source

- P5 `01`; DID v1.9 §10.1/§10.4.1, G1/G6

## Depends On

- P4-018

## Objective

Create the minimal runnable `apps/single-workspace` composition root wiring P1–P4.

## Outputs

- `apps/single-workspace` package + `src/composition.ts` Layer graph + `src/main.ts` entry.
- Durable file DB; deterministic Fake Provider; `SliceRuntime` start/stop lifecycle.

## Must Hold

- Composition Root only; production packages never depend on `apps/*`.
- Durable file DB (not `:memory:`) for restart continuity.
- Fake Provider wired; no live network required.

## Must Not Decide

- No subsystem redesign.
- No production daemon/CLI surface.
- No verification/dependency/multi-workspace wiring.

## Acceptance

- `pnpm build`/`typecheck` green; the app starts and stops against a durable DB.

## Verification

```bash
pnpm test p5-composition
pnpm check
```
