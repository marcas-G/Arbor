# P8-002 — P8 DDL (four tables) + repositories + M-4 settlement workId

## Source

- P8 `00` §B (items 1/7, table naming), `01` §1/§4, `04` §1/§2; DID v1.11 §9.3, §12.8 C7

## Depends On

- P8-001

## Objective

Migration 8: the four P8 tables with DID §9.3 names — `verifications` /
`verification_executions` / `verification_evidence` / `work_acceptances` — their
repositories, and the M-4 settlement-payload `workId` resolution.

## Outputs

- Migration 8 + `VerificationRepository` / `AcceptanceRepository` / `EvidenceRepository` + settlement workId resolution.

## Must Hold

- Table names exactly per DID §9.3; missionSnapshot / criteriaResults stored as row JSON (`04` §2).
- **One-Open CAS**: persistence enforces at most one Open verification per `(workId, targetWorkRevision)` (partial unique index — the P7 dependencies-table CAS precedent); concluded history unrestricted.
- `work_acceptances` **double uniqueness**: `acceptanceId` unique AND `(workId, targetWorkRevision)` unique (at most one acceptance row per revision).
- `verification_evidence` **append-only + immutable**: repository exposes no update/delete path.
- **M-4 settlement workId**: the P5 settlement record (currently workRevision/claimRef only) resolves and persists `workId`; the replay path reads it.
- Owner snapshot (owning Workspace at StartVerification) is a persisted column (G4).

## Must Not Decide

- No handler semantics; no wake production; no Program text.

## Acceptance

- Migration round-trip green; uniqueness violations mechanically rejected; append-only enforced by the repository surface (exported-symbol allowlist).

## Verification

```bash
pnpm test p8-ddl
pnpm check
```
