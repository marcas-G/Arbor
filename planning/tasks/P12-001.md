# P12-001 — Plugin SDK/SPI + compatibility + Project-tool registration/trust + architecture allow-list reconciliation

## Source

- docs/design/implementation/P12/01-plugin-sdk-spi.md §1–§5, §7

## Depends On

- P11-013

## Objective

Plugin categories (in-process adapter / declarative / remote), `PluginId`/`PluginVersion`/`PluginSdkApiVersion`, compatibility policy (semver/deprecation), SPI surface, `ProjectToolRegistry` + `RegisterProjectToolAuthority` + content/version-bound trust, registered→catalogued→visible; plus `01` §7 architecture allow-list reconciliation (remove stale `verification-runtime`; relocate `fingerprintOf`→`ports` and replace `CommandRejection` with a `ports`-level type so `environment-resolver-local`/`sandbox-worktree` are `domain, ports` only).

## Outputs

`packages/ports` + `packages/application` plugin/registration surfaces; `tests/architecture/package-dag.ts` reconciliation; suites `p12-plugin-sdk`, `p11-closure` (amended).

## Must Hold

- `01` §4/§5/§7; plugin forms are adapters/declarative/remote (no new package category); project-local not auto-trusted; registration is a governance Command (CI-1); adapter edges reconcile to DID §10.4.1.
- Blocker: B9

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization. A digest-recipe / fingerprint-semantics change from relocating `fingerprintOf` must be recorded as a governance item (DID gap), not invented.

## Acceptance

- EC-2 mechanically checked (see `14-acceptance.md` §3.1/§3.2) ; blocker B9 evidenced.

## Verification

```bash
pnpm test p12-plugin-sdk
pnpm check
pnpm test p11-closure
```
