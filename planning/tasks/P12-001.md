# P12-001 — Plugin SDK/SPI + compatibility + Project-tool registration/trust

## Source

- docs/design/implementation/P12/01-plugin-sdk-spi.md

## Depends On

- P11-013

## Objective

`PluginId`/`PluginVersion`/`PluginSdkApiVersion`, compatibility policy (semver/deprecation), plugin categories, `ProjectToolRegistry` + `RegisterProjectToolAuthority`, content/version-bound trust, registered→catalogued→visible.

## Must Hold

- `01` §4/§5/§7; plugin categories are adapters/declarative/remote (no new package); project-local not auto-trusted; registration is a governance Command (CI-1).

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- Owning contract assertions mechanically checked; any P12 completion blocker touched by this task is evidenced.

## Verification

```bash
pnpm test p12-plugin-sdk
pnpm check
```
