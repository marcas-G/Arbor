# P12-002 — Authority Resolver production plane + PermissionGrantRepository

## Source

- docs/design/implementation/P12/02-authority-resolver.md

## Depends On

- P11-013

## Objective

`AuthorityResolverPort` (resolve/resolveInvocation/resolveApproval, `R = never`), exact-bound trusted facts, `PermissionGrantRepository` + `GrantPermission`/`RevokePermission`, P2 External widening.

## Must Hold

- `02` §2A/§3/§4/§5; resolver is pure/deterministic and MUST NOT invoke CommandGateway / mutate / consume InvocationApproval / execute tools; declared in `packages/application`.

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- Owning contract assertions mechanically checked; any P12 completion blocker touched by this task is evidenced.

## Verification

```bash
pnpm test p12-authority-resolver
pnpm check
```
