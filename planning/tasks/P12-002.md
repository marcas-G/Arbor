# P12-002 — Authority Resolver production plane + PermissionGrantRepository

## Source

- docs/design/implementation/P12/02-authority-resolver.md §1–§6

## Depends On

- P12-001

## Objective

`AuthorityResolverPort` (resolve/resolveInvocation/resolveApproval, `R = never`), exact-bound trusted facts, P2 `External` fact-union widening, `PermissionGrantRepository` port + `permission_grants` migration `0012` + `GrantPermission`/`RevokePermission`; production of `RegisterProjectToolAuthority` (`01` §5.1).

## Outputs

`packages/application` resolver + `packages/ports` grant repository; migration `0012_permission_grants`; suite `p12-authority-resolver`.

## Must Hold

- `02` §2A/§3/§4/§5; resolver is pure/deterministic and MUST NOT invoke CommandGateway / mutate / consume InvocationApproval / execute tools; declared in `packages/application`; `put`/`revoke` only via the gateway handlers (CI-1).
- Blocker: B4

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-3 mechanically checked (see `14-acceptance.md` §3.1/§3.2) ; blocker B4 evidenced.

## Verification

```bash
pnpm test p12-authority-resolver
pnpm check
```
