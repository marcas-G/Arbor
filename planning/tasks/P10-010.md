# P10-010 — HumanInterventionApplied emission wiring (steer back-fill, governance four, dormant Stop)

## Source

- P10 `06` §2 (emission points + same-transaction pairing), §3 (consumption); DID v1.13 G7, §8.4A; P6 `04` §2 (SteerWork handler, WorkSteered payload); P2 `01` (StopExecution); P7 `06`/P8 (WithdrawDependency / MarkDependencyUnfulfillable / AcceptWorkOutcome handlers)

## Depends On

- P10-001

## Objective

Wire the same-transaction emission of `HumanInterventionApplied`: the
SteerWork handler (with the WorkSteered payload back-fill), the four
human-originated governance commands, and the dormant wire-only Stop
branch.

## Outputs

- Emission wiring in SteerWork + four governance handlers; dormant Stop branch (declared, payload-shaped, uninvoked); journal-level emission tests; `p10-intervention` suite.

## Must Hold

- **WorkSteered P6 形状补发**: the back-fill is **inherited evolution — P6 not reopened**; the handler already pairs event emission; the fix equips both frozen payload shapes (`WorkSteered {workId, fromRevision, toRevision, severity}` per P6 `04` §2 and the new fact) in the **same semantic transaction** as the successful mutation.
- **GovernanceDecision emission set = exactly the four human-originated mutating governance commands** (R8): RecordDecision (formation approval), AcceptWorkOutcome, WithdrawDependency, MarkDependencyUnfulfillable — each emits the fact **only when the submitting principal is human-originated** (provenance AuthenticatedHuman, DID §8.4A); **agent-originated submissions emit nothing**.
- **Stop 分支 dormant wire-only**: the emission point is the StopExecution submission path for human-originated stops, **preconditioned on resolver-admitted (P12, GQ4)**; until the resolver lands the branch is **dormant by frozen design** — P10 asserts it wire-only (declaration + payload shape checked); **no human-stop mutation is ever invoked**.
- Fire-once per successful mutation — idempotent by command receipt; P10 only consumes downstream (bubbling + audit timeline render per `06` §3).

## Must Not Decide

- No new governance semantics; no resolver; no P6/P2 contract reopening (additive emission only); no automatic emission on agent-originated submissions.

## Acceptance

- Steer / critical-steer / formation-approval / accept / withdraw / mark-unfulfillable (human principal) → paired facts emitted same-transaction with payloads per `06`; agent-principal fixtures emit nothing; Stop branch: declaration + payload shape asserted with zero mutations; replay idempotency green.

## Verification

```bash
pnpm test p10-intervention
pnpm check
```
