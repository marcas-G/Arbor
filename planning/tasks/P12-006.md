# P12-006 — Remote Worker transport / identity / mediation

## Source

- docs/design/implementation/P12/06-remote-worker.md

## Depends On

- P11-013

## Objective

`WorkerId`+`WorkerIncarnationId`, lease incarnation migration `0011` + fence triple, `RemoteWorkerTransportPort`, `RemoteWorkerMediationPort` delegating to `CommandGateway`, authenticated peer identity binding.

## Must Hold

- `06` §2-§6; single-writer control plane; wire DTO carries no authority fact; no direct canonical DB write (CI-1).

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- Owning contract assertions mechanically checked; any P12 completion blocker touched by this task is evidenced.

## Verification

```bash
pnpm test p12-remote-worker
pnpm check
```
