# P12-006 — Remote Worker transport / identity / mediation

## Source

- docs/design/implementation/P12/06-remote-worker.md §1–§6

## Depends On

- P11-013
- P12-001 (shared `ALLOWED_EDGES` / migration list)
- P12-002 (migration list assembly)

## Objective

`WorkerId`+`WorkerIncarnationId`, lease incarnation migration `0011` + fence/CAS triple, `RemoteWorkerTransportPort` + `RemoteWorkerMediationPort` (delegating to `CommandGateway`), authenticated peer identity binding, wire DTO without an authority fact.

## Outputs

`packages/ports` wire contract + `packages/application` mediation port + `adapters/worker-transport`; migration `0011_lease_worker_incarnation`; suite `p12-remote-worker`.

## Must Hold

- `06` §2–§6; single-writer control plane; no direct canonical DB write (CI-1); `ports` wire contract references no `packages/application` type.
- Blocker: B8

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-7 mechanically checked (see `14-acceptance.md` §3.1/§3.2) ; blocker B8 evidenced.

## Verification

```bash
pnpm test p12-remote-worker
pnpm check
```
