# P12-004 — Observability / health / usage plane

## Source

- docs/design/implementation/P12/04-observability-health-usage.md §1–§6

## Depends On

- P12-001, P12-002, P12-006

## Objective

Observability module + `HealthPort.readiness` (`PersistenceHealthProbe`), `UsageCost` ADT + `UsageService.derive`, versioned pricing, architecture boundary test; readiness baseline `max(P12_MIGRATIONS)=13`.

## Outputs

`packages/projection-runtime/src/observability/**` + `packages/ports` `HealthPort`; `tests/architecture/p12-observability-boundaries.test.ts`; suite `p12-observability`.

## Must Hold

- `04` §2–§6; derived operational state only (CI-3); unknown cost = `Unknown`/`None`, never `0`; no scheduler/admission telemetry import. Depends on P12-001/002/006 for the migration baseline (max 13).
- Blocker: B6

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-5 mechanically checked (see `14-acceptance.md` §3.1/§3.2) ; blocker B6 evidenced.

## Verification

```bash
pnpm test p12-observability
pnpm check
```
