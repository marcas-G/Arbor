# P3-011 — ProviderRuntime + fake provider + failure model

## Source

- P3 `01` §1–§7/§8–§9; DID v1.7 §6A.8/§6A.9/§9.10/§9.11

## Depends On

- P3-003

## Objective

Implement the provider runtime, a fake adapter, and the failure model.

## Outputs

- `ProviderRuntimeLive` (transport/auth/stream/timeout/safe-retry/normalization/usage).
- `adapters/provider-fake` deterministic `ProviderPort`.
- `provider_turns`/`provider_attempts` persistence; `ModelCapabilityPort`.

## Must Hold

- Turn intent + manifest before the request.
- Retry does not create a Turn or bump `turnNo`.
- Failure kinds per `01` §6; SDK errors translated.

## Must Not Decide

- No `AgentDirective` in provider events.
- No live network in tests.

## Acceptance

- Fake provider streams the frozen ADT; retries recorded as attempts.

## Verification

```bash
pnpm test p3-provider
pnpm check
```
