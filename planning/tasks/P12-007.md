# P12-007 — ToolCatalogPort model-facing resolution + compiler + rename migration

## Source

- docs/design/implementation/P12/07-toolcatalog-model-facing.md §1–§3

## Depends On

- P12-001

## Objective

`ModelFacingToolDefinition` (description/schema/version/capabilityMetadata/sideEffectSemantics), `visibleRefs` + `resolveForModel`, compiler emits real schema/description, rename migration of call sites/suites.

## Outputs

`packages/ports` ToolCatalogPort revision + `packages/model-context` compiler; migrated call sites/suites; suite `p12-toolcatalog`.

## Must Hold

- `07` §2–§3; DID §7.6 authoritative; model-context `!-> tool-runtime`; no placeholder (CI-6); rename migration keeps `pnpm check` green.
- Blocker: B2

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-8 mechanically checked (see `14-acceptance.md` §3.1/§3.2) ; blocker B2 evidenced.

## Verification

```bash
pnpm test p12-toolcatalog
pnpm check
```
