# P12-007 — ToolCatalogPort model-facing resolution + compiler

## Source

- docs/design/implementation/P12/07-toolcatalog-model-facing.md

## Depends On

- P11-013

## Objective

`ModelFacingToolDefinition` (description/schema/version/capabilityMetadata/sideEffectSemantics), `visibleRefs` + `resolveForModel`, compiler emits real schema/description, rename migration of call sites.

## Must Hold

- `07` §2-§3; DID §7.6 authoritative; model-context `!-> tool-runtime`; no placeholder (CI-6).

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- Owning contract assertions mechanically checked; any P12 completion blocker touched by this task is evidenced.

## Verification

```bash
pnpm test p12-toolcatalog
pnpm check
```
