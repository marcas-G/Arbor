# P2-017 — Architecture tests for P2 packages

## Source

- DID v1.7 §10.4.1; P1-015

## Depends On

- P2-001
- P2-003

## Objective

Extend the architecture tests to the P2 packages.

## Outputs

- `ALLOWED_EDGES` entries for `execution-runtime`, `agent-runtime`, `testkit` (already present) and `worker-local` adapter.
- Tests: `execution-runtime -> domain, ports, application, agent-runtime`; `runtime !-> adapters`; `adapters !-> runtime`; no deep imports; no catch-all error / service locator.

## Must Hold

- Checker stays data-driven and non-vacuous.
- New packages covered automatically.

## Must Not Decide

- No weakening of the matrix.

## Acceptance

- `pnpm architecture` green for the P2 package set.
- Synthetic manifests still fail on forbidden edges / unknown packages.

## Verification

```bash
pnpm test p2-architecture
pnpm check
```
