# P12-005 — StorageScaleAssessment + DurabilityEnvelope + restore drill + snapshot pruning + P11 convergence

## Source

- docs/design/implementation/P12/05-storage-scale-durability.md §1–§5

## Depends On

- P12-001, P12-002, P12-006

## Objective

`assessStorage` + checked-in `planning/results/P12.storage-assessment.json`, `DurabilityEnvelope` + backup/restore + `pnpm drill:restore` artifact, snapshot pruning/retention, P11 convergence (`advanceAnchor` removal + `p11-closure.test.ts`/`p11-revision-algebra.test.ts`/`p11-controlbasis.test.ts`/`p11-acceptance.test.ts` amendments; `parseSnapshotBlob` structured encoding).

## Outputs

`packages/application`/`domain` storage + drill script + `planning/results/P12.storage-assessment.json`/`P12.restore-drill.json`; suites `p12-storage`, `p11-closure`, `p11-revision-algebra`, `p11-controlbasis`, `p11-acceptance` (amended).

## Must Hold

- `05` §2–§5; SQLite default; PostgreSQL only if envelope unmet; migration baseline `max(P12_MIGRATIONS)=13`; drill isolated + post-restore fencing (SD §10.5; DID §6.3).
- Blocker: B7

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-6 mechanically checked (see `14-acceptance.md` §3.1/§3.2) ; blocker B7 evidenced.

## Verification

```bash
pnpm test p12-storage
pnpm test p11-revision-algebra
pnpm test p11-controlbasis
pnpm test p11-acceptance
pnpm check
pnpm drill:restore
```
