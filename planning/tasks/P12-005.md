# P12-005 — StorageScaleAssessment + DurabilityEnvelope + restore drill + snapshot pruning

## Source

- docs/design/implementation/P12/05-storage-scale-durability.md

## Depends On

- P11-013

## Objective

`assessStorage` + checked-in assessment artifact, `DurabilityEnvelope` + backup/restore + `pnpm drill:restore` artifact, snapshot pruning/retention, P11 convergence items (`advanceAnchor`, `parseSnapshotBlob`).

## Must Hold

- `05` §2-§5; SQLite default; PostgreSQL only if envelope unmet; `p11-closure.test.ts` amended (TR-1); migration baseline `max(P12_MIGRATIONS) = 13`.

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- Owning contract assertions mechanically checked; any P12 completion blocker touched by this task is evidenced.

## Verification

```bash
pnpm test p12-storage
pnpm check
```
