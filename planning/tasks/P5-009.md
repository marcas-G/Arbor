# P5-009 — Full vertical-slice acceptance story

## Source

- P5 `05` (all); DID v1.9 G1/G4/G6/G7

## Depends On

- P5-008

## Objective

Implement the single deterministic end-to-end acceptance story.

## Outputs

- One suite: CreateProject/AssignWork → scheduler → admit → lease → driver → prepareTurn → fake provider → InvokeTool → P4 → Observation → Yield → wait → wake → continuation → CompletionClaim → restart.

## Must Hold

- Work remains Open at the end.
- Both Executions share `execution.sessionId`.
- Deterministic under the Fake Provider; no live network.

## Must Not Decide

- No verification/acceptance.
- No live provider gating.

## Acceptance

- All `05` §2 assertions pass; a `DirectiveUnsupported` turn is exercised.

## Verification

```bash
pnpm test p5-slice-acceptance
pnpm check
```
