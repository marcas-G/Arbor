# P12-008 — Runtime Safety Envelope six-dimension completion

## Source

- docs/design/implementation/P12/08-runtime-safety-completion.md §1–§7A

## Depends On

- P11-013

## Objective

D1–D6 with the eight evidence facets; `RuntimeSafetyPolicy` + `RuntimeSafetyGateLive`; `RuntimeSafetyObservation` (retryCount/durableProgress/leaseGeneration/chainDepth/inFlight); P2 evolution filed.

## Outputs

`packages/ports` observation channel + `packages/execution-runtime` gate; suite `p12-runtime-safety`.

## Must Hold

- `08` §2–§7A; violation → `Interrupted(RuntimeSafetyStop)`, Work Open; P9 `03` §4 no-durable-upgrade (CI-4); `admitActivity` widening filed as an inherited P2 evolution.
- Blocker: B3

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-9 mechanically checked (see `14-acceptance.md` §3.1/§3.2) ; blocker B3 evidenced.

## Verification

```bash
pnpm test p12-runtime-safety
pnpm check
```
