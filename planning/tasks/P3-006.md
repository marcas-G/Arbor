# P3-006 — Context layers / retention / budget

## Source

- P3 `02` §5; DID v1.7 §8.8–§8.10

## Depends On

- P3-005

## Objective

Implement C0–C6 layers, retention/eviction and the context budget.

## Outputs

- `ContextFragmentRef`, layer classification, `RetentionClass`, eviction order.
- Budget `B_ctx = B_model − B_output − B_protocol − B_tools`.
- `ContextUnsatisfiable` on Pinned+Protected overflow.

## Must Hold

- C0/C1 pinned.
- Never silently drop hard control facts.
- Output reserved before input.

## Must Not Decide

- No numeric defaults as contract.
- No Memory persistence policy.

## Acceptance

- Pinned overflow → `ContextUnsatisfiable`, not truncation.

## Verification

```bash
pnpm test p3-context
pnpm check
```
