# P6-014 — Acceptance stories A–F + architecture + P5 guard

## Source

- P6 `06` 全部; P5 `05`（回归护栏）

## Depends On

- P6-013

## Objective

Implement the six deterministic acceptance stories（Fake Provider，durable DB，扩展既有 composition root）plus P6 architecture tests and the P5 regression guard.

## Outputs

- `tests/acceptance/p6-*.test.ts` 六故事 + `tests/architecture/` P6 edges.

## Must Hold

- Every assertion of `06` §1–§7 mechanically checked（含 D1–D4 专项：两步分离、stale 拒绝、幂等重投、Report 负断言、Inbox 重放恰一条、Session 零写入、版本门）.
- depth 判定无模型参与的单元证明; Normal/Critical 差异由动作计数判定.
- P5 acceptance stories pass unchanged; production packages never depend on `apps/*`.

## Must Not Decide

- No new runtime; no real humans/models as gating conditions.

## Acceptance

- Stories A–F green in one durable-DB run; `pnpm architecture` green.

## Verification

```bash
pnpm test p6-acceptance
pnpm test p6-architecture
pnpm architecture
pnpm check
```
