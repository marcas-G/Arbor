# P10-004 — Attention read-model: six sources / dedup / bubbling + GAP-01 derived view

## Source

- P10 `02` 全部 (six-source mapping, bubbling, read-model contract, GAP-01); `planning/gaps/P7-GAP-01.md`; P7 `05` §2 (No.42 project-idle gate); P9 `01`/`04` (ReconciliationEscalated); P8 `02` §3 (verifier orphan derived condition); P6 `01` §4 (formation approval pending); DID v1.13 G3/G8; SD §12.3, §14 No.42/55

## Depends On

- P10-003

## Objective

The materialized Attention read-model (incremental via the generic
consumer, P1 `05` P9-hardened) plus the on-read derived GAP-01 view, with
subtree bubbling integrated into the Tree view's `subtreeAttention`
summaries.

## Outputs

- Attention read-model rows + consumer wiring; dedup identities; bubbling integration into Tree; `WaitingOnVacantProducer` live-join view; `p10-attention` suite.

## Must Hold

- Six-source mapping **exactly per `02` §1**: `DependencyMarkedUnfulfillable` → Attention → consumer workspace; `DeadlockAttentionRequested` → renders **Action Required** (emitted only under the No.42 project-idle gate — every emitted deadlock fact is already Action Required; **no conditional branch exists**); safety-stop record / `Interrupted(RuntimeSafetyStop)` → Attention → execution workspace; `ReconciliationEscalated` → Action Required → execution workspace; verifier orphan (open verification × settled executions, P8 `02` §3 derived) → Attention → owner workspace; vacant producer → Attention → consumer workspace (label `WaitingOnVacantProducer`).
- Normal = the **absence of facts** (default state, not a row); human-required approval (formation approval pending) maps to Action Required.
- Dedup on read keyed by the frozen identities: ReconciliationEscalated `executionId + invocationRefsFingerprint`; Deadlock cycle fingerprint; derived views their natural join key. Bubbling aggregates **deduplicated facts, not raw events**.
- Bubbling: subtree summary = aggregated severity counts over descendants; **context is never copied upward** (SD §12.3).
- **Attention 零 mutation (GQ3/G8)**: the read-model never performs canonical mutation; disposition of any attention row is a human/parent governance command (S3 step 12: Withdraw / MarkUnfulfillable / steer / stop) — snapshot assertion.
- **GAP-01 predicate**: `dependencies(Unsatisfied ∧ WorkspaceBound) × works(no Open Work) ∧ ¬Retired` — the **retired-workspace negative fixture is mandatory**; on-read derived (live join, no materialization); view label `WaitingOnVacantProducer`; may **not** be silently descoped (gap file's non-blocking declaration).

## Must Not Decide

- No new severities beyond SD's three; no automatic escalation actions; no notification transport (P12); no new runtime events for the vacant-producer channel.

## Acceptance

- Fixtures for all six sources (incl. GAP-01 retired negative case) → severities/targets/bubbling/dedup exactly per `02`; canonical-state snapshot unchanged; rebuildable per `04` (deferred detail to P10-009).

## Verification

```bash
pnpm test p10-attention
pnpm check
```
