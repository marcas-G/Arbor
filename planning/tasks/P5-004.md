# P5-004 — Slice command handler registry wiring

## Source

- P5 `01`; P2 `02`, P3 `03`

## Depends On

- P5-001

## Objective

Wire the P2/P3 command handler registry + gateway into the composition root.

## Outputs

- A combined registry providing `AdmitExecution`/`StopExecution`/`SettleExecution` (and P1 commands where needed).

## Must Hold

- Reuse the frozen P2/P3 registries; no new command semantics.
- Gateway + fence/stop hook wired.

## Must Not Decide

- No new commands.
- No handler behavior change.

## Acceptance

- The gateway resolves the slice's command set.

## Verification

```bash
pnpm test p5-slice-registry
pnpm check
```
