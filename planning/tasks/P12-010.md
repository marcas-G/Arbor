# P12-010 — Transport shells + daemon/recovery/consumer-loop surfaces

## Source

- docs/design/implementation/P12/10-transport-shells.md §1–§4

## Depends On

- P12-002, P12-004, P12-005, P12-006, P12-008

## Objective

HTTP/WS/CLI/web shells binding `api-contracts`, auth → resolver at composition root, production daemon + recovery daemon/composition surface + consumer-loop daemon wiring, Search deferral, worker transport cross-ref.

## Outputs

`apps/*` transport shells + daemon wiring; suite `p12-transport`.

## Must Hold

- `10` §1–§4; no view-semantics reinterpretation; no authority decision in transport; Search is an out-of-v1 deferral (no Search surface).

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-11 mechanically checked (see `14-acceptance.md` §3.1/§3.2).

## Verification

```bash
pnpm test p12-transport
pnpm check
```
