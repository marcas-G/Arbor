# P12-012 — Security hardening + performance

## Source

- docs/design/implementation/P12/13-security-performance.md §1–§5

## Depends On

- P12-002, P12-003, P12-005, P12-006

## Objective

Information Trust Plane enforcement (`ContextFragment.provenance`, DataOnly tagging, `canRaiseAuthority`), sandbox trajectory, sandbox env allow-list negative assertion, performance envelope + measurement method (05-owned artifact).

## Outputs

`packages/model-context` context boundary + `packages/ports` fragment revision; suite `p12-security-performance`.

## Must Hold

- `13` §2–§5; provenance is source-breaking (MAJOR) at the model-context boundary; no new isolation semantics; envelope artifact owned by P12-005; numbers empirical per DID §13.

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-13 mechanically checked (see `14-acceptance.md` §3.1/§3.2).

## Verification

```bash
pnpm test p12-security-performance
pnpm check
```
