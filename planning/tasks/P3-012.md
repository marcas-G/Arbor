# P3-012 — `decodeTurn` + `AgentDirective` + Output Contract

## Source

- P3 `03` §3–§4, `01` §3; DID v1.7 §8.15/§8.3

## Depends On

- P3-009
- P3-011

## Objective

Decode provider events into a validated `AgentDirective`.

## Outputs

- `AgentDirective` ADT + `decodeTurn`.
- Output Contract binding + validation.

## Must Hold

- `ToolCallProposed` is not itself a directive.
- Every directive carries `decisionBasisManifestId`.
- Canonical-truth directives go through `CommandGateway`.

## Must Not Decide

- No tool authorization/execution (P4).

## Acceptance

- Valid output decodes; contract violation → `ModelOutputContractViolation`.

## Verification

```bash
pnpm test p3-decode
pnpm check
```
