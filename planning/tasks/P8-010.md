# P8-010 — P9 + P14 Program v1 + dual versioning + eval gates + M-2 placeholder migration

## Source

- P8 `02` §4, `05` §2, `04` §5; DID v1.11 §8.4/§8.4A, G6

## Depends On

- P8-008

## Objective

The Verification Program (P9) and Query/Inspection Program (P14) v1 texts with D4
dual versioning, behavioral eval gates, and the M-2 placeholder-mission migration.

## Outputs

- Two Program v1 artifacts + manifests + eval suites + placeholder mission validation.

## Must Hold

- **P9 v1 mandatory clauses** (`02` §4): investigation-plan drafting; evidence-first ordering (tool observation > document > hypothesis); honest criterion-level three-valued reporting ("no problem found" ≠ PASS); boundary-case/counterexample construction duty; UNKNOWN's insufficient-evidence wording duty; prohibition of self-instructions modifying Producer results.
- **P14 v1 mandatory clauses** (`05` §2): read-only discipline; source citation; results are Messages/observations — never canonical mutations. P14 owned and frozen by P8; P10/P12 consume only (G6).
- **D4 dual versioning** (P6 `05` pattern): contractRevision governance gate + textVersion + hash + forced eval rerun on change; Manifest references the hash (P3 pattern).
- **Eval gate per program** (Fake Provider, deterministic): E1 structure adherence (payloads land in frozen ADTs); E2 criterion recall (full coverage / honest three-values); E3 prohibition clauses (no unauthorized mutation / no auto-PASS); E4 version regression. Hash/tamper/version gates mechanically enforced (P6 D4 mode copied).
- **P14 read-only negative**: a query result attempting canonical mutation → violation.
- **M-2 placeholder migration** (`04` §5): legacy `"p6-placeholder"` missions fail StartVerification schema validation → typed rejection (mission invalid); existing Works require RefineWork to supply a real mission; **no auto-fill** (mission is Producer/Parent semantic responsibility). Minimal schema: goal non-empty, ≥1 required criterion, requirement text per criterion.

## Must Not Decide

- No new Program families; no P10/P12 consumption semantics; no eval thresholds beyond the frozen gates.

## Acceptance

- Hash/tamper/version gates green; P14 read-only negative green; placeholder typed rejection green.

## Verification

```bash
pnpm test p8-programs
pnpm check
```
