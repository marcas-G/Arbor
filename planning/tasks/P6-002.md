# P6-002 — P6 DDL migrations

## Source

- P6 `01` §4.1, `02` §3/§4; DID §12.10（store 列）

## Depends On

- P6-001

## Objective

Add the P6 tables: formation proposal store（DecisionRepository 族）, message store（MessageSent 事实）, inbox projection（未消费输入集合）.

## Outputs

- Migration `000N_p6_formation_communication.sql` + repository wiring.

## Must Hold

- Inbox admission is upsert-by-key（messageId / settlement dedup key）.
- Proposal revision/state columns enforce terminal-state + revision monotonicity at L3.

## Must Not Decide

- No semantics beyond contract storage needs.

## Acceptance

- Migration applies/reverts cleanly; repository round-trip tests pass.

## Verification

```bash
pnpm test p6-ddl
pnpm check
```
