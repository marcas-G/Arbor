# P6-006 — Capability-ceiling validate-only checks

## Source

- P6 `03` §2; SD §8.2; 不变量 13/14; P4 v1.8 G4 先例

## Depends On

- P6-005

## Objective

Enforce the four validate-only checks on formation boundaries: subset boundary, policy tightening, no authority amplification, no recursive grant delegation.

## Outputs

- Pure validation module wired into the CreateChildWorkspace chain.

## Must Hold

- Failures → `DomainError.AuthorityDenied`（with reason）; Execution not failed.
- Validate-only: no ownership mutation here（governance commands own that）.
- `basisResponsibilityRevision` precondition（P1 §6）remains the existing special case; no double-check divergence.

## Must Not Decide

- No Authority Resolver semantics; no ResourceOwnership changes.

## Acceptance

- Each of the four checks has positive and negative cases（正/反例）.

## Verification

```bash
pnpm test p6-ceiling
pnpm check
```
