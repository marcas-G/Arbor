# P10-001 — Domain & event-face evolution (HumanInterventionApplied, WorkSteered back-fill, ViewId/status vocabulary)

## Source

- P10 `06` §1 (frozen payload), §2 (emission pairing table — payload shape only here); DID v1.13 G7, §5.3 payload note, §8.4A (AuthenticatedHuman); P6 `04` §2 (WorkSteered frozen payload — back-fill); P10 `01` §1 (ViewId must-list), §2 (WorkspaceStatus label map); P10 `05` §1 (ViewId enumeration source)

## Depends On

- P9-014

## Objective

Equip the domain event faces and projection vocabulary the whole phase
builds on: the `HumanInterventionApplied` payload, the back-filled
`WorkSteered` payload (P6 frozen shape — the empty-shell events are the
recorded implementation deviation, DID v1.13 G7), the `ViewId` union, and
the WorkspaceStatus label union.

## Outputs

- Domain event payload types + catalog entries; `ViewId` union (`01` must-set, Search excluded); WorkspaceStatus label union (six labels); `p10-domain-events` suite.

## Must Hold

- `HumanInterventionApplied` payload **exactly** per `06` §1: `{actor: Principal, targetWorkspaceId: WorkspaceId, summaryRef: ContentRef, occurredAt: string, kind: "Steer" | "CriticalSteer" | "Stop" | "GovernanceDecision"}` — no extra fields.
- `WorkSteered` back-fill = the P6 `04` §2 frozen shape `{workId, fromRevision, toRevision, severity}` — **inherited evolution, P6 not reopened**.
- Label union frozen from SD §12.2 text: `executing / waiting-runnable / waiting-blocked / idle / retired / attention-flagged` — no invented labels; `retired` is terminal (GAP-01 negative fixture lands there); `attention-flagged` is a composable overlay.
- `ViewId` = the `01` §1 must-set (Search absent — deferred with rationale).
- Domain stays pure (`R = never`); exhaustive-switch and illegal-construction tests compile under `pnpm typecheck`.

## Must Not Decide

- No emission wiring (P10-010); no DTO/transport shapes or port signature (P10-002); no label-to-facts derivation (P10-003); no new governance semantics; no domain enum changes behind the labels.

## Acceptance

- Payload/shape round-trip + exhaustive `kind`/label/ViewId switch tests green; catalog entries versioned; no production behavior change beyond the equipped payloads.

## Verification

```bash
pnpm test p10-domain-events
pnpm check
```
