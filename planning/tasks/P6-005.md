# P6-005 — Deep-layer formation + authority fact projection

## Source

- P6 `01` §4.3, `03` §3; P1 `01` §2A; DID v1.7 G1（authority facts）

## Depends On

- P6-001

## Objective

Implement gate-free deep-layer formation: directive handler → authority fact projection from Execution context → `CreateChildWorkspace`（→ `AssignWork`）.

## Outputs

- Directive handler + `WorkspaceAgentAuthority` projection helper.

## Must Hold

- depth(parent != Root) skips proposal entity entirely; depth computation is pure structure, no model calls.
- `authority.workspaceId == payload.parentWorkspaceId`; Retired parent → precondition rejection（DID §1.4A）.
- Sibling/cross-parent attempts → `AuthorityDenied`, Execution continues（non-fatal observation）.

## Must Not Decide

- No ceiling checks (P6-006); no specialist path.

## Acceptance

- Event sequences match `01` §4.3; typed rejections for authority violations.

## Verification

```bash
pnpm test p6-deep-formation
pnpm check
```
