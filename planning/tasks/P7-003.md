# P7-003 — DeclareDependency handler + directive

## Source

- P7 `01` §2, §9; DID v1.10 §8.15/§8.16; P5 `03` §2 (retired row)

## Depends On

- P7-002

## Objective

Discharge the last `DirectiveUnsupported` row of P5 `03` §2: the `DeclareDependency` handler (payload / preconditions / rejections / event / authority fact) plus its directive realization.

## Outputs

- Command handler + directive handler projecting `DeclareDependencyAuthority`.

## Must Hold

- Rejection rows exactly `01` §2: `WorkNotFound`, `TerminalLifecycleMutation`(Work/Workspace), `RevisionConflict`, `AuthorityDenied` — including cross-Project `WorkspaceBound` / `WorkBound` targets.
- Emits `DependencyDeclared { dependencyId, consumerWorkId, producerBinding, expectedDeliverable, revision }` (coordinator re-check trigger, `04` §1).
- DeclareDependency does not imply Yield — no WorkWait side effect (DID §8.16).
- Command rejections surface as directive execution results / model-visible observations, never Execution failures.

## Must Not Decide

- No matcher / state-machine changes; no coordinator behavior (P7-009); no DDL.

## Acceptance

- Rejection table covered row by row; directive path allocates ids, reads the observed Work revision, and submits via `CommandGateway`.

## Verification

```bash
pnpm test p7-declare-dependency
pnpm check
```
