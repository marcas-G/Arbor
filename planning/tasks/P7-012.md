# P7-012 — Acceptance stories A–F + architecture + guards

## Source

- P7 `07` 全部; P5 `02` / P6 suites (regression guards)

## Depends On

- P7-003 … P7-011

## Objective

Implement the six deterministic acceptance stories（Fake Provider / direct command submission, one durable DB, existing composition root）plus P7 architecture tests and the P5/P6 regression guards.

## Outputs

- `tests/acceptance/p7-*.test.ts` six stories + `tests/architecture/` P7 edges.

## Must Hold

- Every assertion of `07` §1–§7 mechanically checked: six-command event/rejection sets; Deliver wake/Inbox/non-satisfaction; classification matrix; coordinator idempotence / dead-letter / single command face; deadlock fact + zero mutation; wake commit boundary + observedRevision progression; satisfaction immutability + No.49 traceability; No.20 zero model calls when no runnable work.
- P5 runnable-source suite runs under supersession semantics; all other P5/P6 suites unchanged-green.
- Architecture tests enforce P7 module placement; composition uses P7_MIGRATIONS; production packages never depend on `apps/*`.

## Must Not Decide

- No real models/humans as gating; no P8 assertions (verification vocabulary appearing in an assertion is a boundary violation); no UI acceptance (P10).

## Acceptance

- Stories A–F green in one durable-DB run; `pnpm architecture` green.

## Verification

```bash
pnpm test p7-acceptance
pnpm test p7-architecture
pnpm architecture
pnpm check
```
