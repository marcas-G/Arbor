# P10-003 — projection-runtime skeleton + Tree/status views (exhaustive label map incl. retired)

## Source

- P10 `01` §1 (Tree / Workspace Summary / Project Overview rows; derive inputs), §2 (WorkspaceStatus exhaustive label map), §3 (per-view query + read-only derive); P10 `05` §1 (TreeViewReq core; package deps), §3 (P10/P12 boundary); DID §10.4 hard rule, §10.4.1 (projection-runtime deps domain+ports); S2.4.1

## Depends On

- P10-002

## Objective

Stand up the `projection-runtime` package (deps: domain, ports — never
participates in authority) and land the Tree view with the exhaustive
WorkspaceStatus label mapping; Workspace Summary / Project Overview are
aggregated renderings of the Tree view (no separate model). Attention
summaries and usage summaries render zero-valued here and are wired by
P10-004 / P10-007.

## Outputs

- `projection-runtime` package skeleton + read-only derive plumbing; Tree derive + label map; Summary/Overview renderings; `p10-tree-status` suite; architecture edges.

## Must Hold

- Label mapping is an **exhaustive switch** over the frozen six-label map (`01` §2): `executing` ⇔ active main execution; `waiting-runnable` ⇔ no active main ∧ runnable ≠ ∅ (P7 classification); `waiting-blocked` ⇔ no active main ∧ runnable = ∅ ∧ (active WorkWait ∨ blocking dependency ∨ open verification pending); `idle`; `retired` ⇔ lifecycle Retired (**terminal**); `attention-flagged` ⇔ any Attention fact targeting the workspace (overlay, composable).
- waiting-blocked disambiguation **reuses the P7-frozen blocking predicate and P8 one-Open-per-revision facts** — no re-invention.
- Status is a pure projection of canonical facts — **no reverse pressure on Domain enums** (SD §12.2).
- Derive inputs read-only over canonical tables + journal; **no view writes anything** (write-count snapshot in tests; DID §10.4; SD §13.10).
- Tree derive inputs per `01` §1 (workspaces parent chain, works.current, dependencies, verifications, attention read-model, usage summary — the last two land zero-valued here).
- S2.4.1 reading: the tree answers who is responsible / doing / waiting / blocked / needs attention without per-node drill-in.

## Must Not Decide

- No attention fact derivation or bubbling (P10-004); no freshness barrier semantics (P10-005); no transport; no new labels; no Search.

## Acceptance

- Seeded multi-level fixture → Tree query returns the responsibility chain with per-node status for all six labels (incl. a `retired` node); no-write snapshot assertion; exhaustive-switch test green.

## Verification

```bash
pnpm test p10-tree-status
pnpm architecture
pnpm check
```
