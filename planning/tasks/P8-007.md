# P8-007 — Consumer B: WorkOutcomeAccepted → CompleteWork (re-validation)

## Source

- P8 `03` §2; DID v1.11 G3, §5.4

## Depends On

- P8-005

## Objective

The deterministic consumer from `WorkOutcomeAccepted` to `CompleteWork` via
CommandGateway — the symmetric chain-end closure (GQ4/G3).

## Outputs

- Consumer B + offset wiring.

## Must Hold

- `CommandId = deterministic f(workId, targetWorkRevision, verificationId)`; authority = `CompleteWorkAuthority` (System origin, causationRef = eventId).
- **No precondition exemption**: the consumer submits through the same handler — the seven-fold precondition is fully re-validated against current state (G3: Acceptance is the Parent's semantic decision, completion is mechanical closure).
- **Work already Completed → idempotent**: existing Receipt / `TerminalLifecycleMutation` recorded; refine after acceptance → typed rejection recorded, no retry burn.
- Never bypasses the CommandGateway; events never start Agents (commands only).

## Must Not Decide

- No acceptance semantics; no lifecycle changes; no wake production.

## Acceptance

- Deterministic CommandId, full re-validation, and Completed-idempotency mechanically checked.

## Verification

```bash
pnpm test p8-consumer-b
pnpm check
```
