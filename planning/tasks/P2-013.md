# P2-013 — `ExecutionScheduler` + `RunnableWorkSource` stub

## Source

- P2 `02` §7, `05` §1, §4, §8; DID v1.7 §8.18A, G3

## Depends On

- P2-009
- P2-012

## Objective

Implement the deterministic re-evaluation decision table over `RunnableWorkSource`.

## Outputs

- `ExecutionSchedulerLive` (`reevaluate`, `registerWorkWait`, `clearWorkWait`, `scheduleTimer`, `dueTimers`).
- `RunnableWorkSource` deterministic stub.

## Must Hold

- Active main → `Noop`.
- Otherwise apply the `05` §4 table exactly.
- `Admit` uses the `primarySessionId` snapshot + a caller-preallocated `executionId`; the Scheduler issues commands, never writes canonical state.
- Single-flight per `workspaceId` via `BEGIN IMMEDIATE` + partial unique index.
- `SelectCurrentWork` is a decision only (execution owned by P6/P7).

## Must Not Decide

- No runnable/dependency graph (P7).
- No governance command execution.

## Acceptance

- Every `05` §4 row has a passing test.
- Concurrent reevaluation yields at most one admission.

## Verification

```bash
pnpm test p2-scheduler
pnpm check
```
