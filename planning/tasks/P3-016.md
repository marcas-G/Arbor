# P3-016 — Behavioral-eval harness + P3 program eval cases

## Source

- P3 `07` (all); C3/C5

## Depends On

- P3-004
- P3-010
- P3-011

## Objective

Build the shared harness and the P3 program suites.

## Outputs

- `BehavioralEvalHarnessService` + `EvalSuiteRef`/`EvalReport`.
- Deterministic fake-provider runner; P3 program eval cases (Base/Responsibility-bound/Work Execution/Compaction).
- Provenance/revision/hash recording.

## Must Hold

- No live network in CI.
- Per-phase ownership of program text/eval cases/acceptance.
- Contract change → new revision + eval.

## Must Not Decide

- No program text for other phases.
- No acceptance criteria for other phases.

## Acceptance

- P3 suites run deterministically; a regressed revision fails the suite.

## Verification

```bash
pnpm test p3-eval
pnpm check
```
