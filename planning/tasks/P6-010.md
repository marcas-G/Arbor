# P6-010 — Inbox promotion/consumption + correlation + wake + governance routing (D2)

## Source

- P6 `02` §1/§4/§6; SD §7.4–§7.5; 不变量 20/21; P6 `01` §4.2（路由目标）

## Depends On

- P6-009

## Objective

Implement deterministic promotion rules（Reply closes correlation; DecisionRequest triggers reevaluation only; Report has zero canonical effects）, consumption marking, wake emission, and the two `RequestGovernance` minimal routes.

## Outputs

- Promotion module + wake wiring + governance routing（FormationApproval → proposal admission + human Inbox; DecisionRequest → parent Inbox）.

## Must Hold

- **Report produces no Dependency-class events/state changes（D2 负断言）**.
- Promotion never calls a model; arrival never preempts（不变量 21）.
- Inbox != Next Context: Context Builder consumes selectively（SD §7.4）.
- Governance routing itself performs no canonical mutation.

## Must Not Decide

- No satisfaction semantics (P7); no priority/preemption vocabulary.

## Acceptance

- Promotion rules observable; Report negative assertion mechanically checked; governance routes land in correct inboxes.

## Verification

```bash
pnpm test p6-inbox-promotion
pnpm check
```
