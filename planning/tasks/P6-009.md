# P6-009 — SendMessage + Communicate + MessageStore

## Source

- P6 `02` §2/§3/§5; DID §4.2, §12.10（SendMessage→MessageSent）

## Depends On

- P6-002

## Objective

Implement the `SendMessage` command handler（MessageStore 持久化）and the `Communicate` directive handler with outbound routing.

## Outputs

- Handler + rejection set + `MessageDelivered` observation for the sender.

## Must Hold

- Full rejection table of `02` §3 enforced; `CommandRejection.ResourceExhausted` added as P6-frozen enum.
- `MessageSent` + recipient Inbox admission in one reliable commit boundary（不变量 36 模式）.
- correlationId/causationId assigned by Communication Runtime, never model-self-attested.

## Must Not Decide

- No promotion rules (P6-010); no Deliver semantics (P7).

## Acceptance

- Rejection table cases all typed; sender observation recorded without implying recipient cognition.

## Verification

```bash
pnpm test p6-send-message
pnpm check
```
