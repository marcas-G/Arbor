# P4-003 — P4 DDL migrations

## Source

- P4 `06` §1, `07` §3

## Depends On

- P4-002

## Objective

Add migration `0004_tool_runtime`.

## Outputs

- `tool_invocations` (with `side_effect_semantics` snapshot).
- `artifacts`.
- `invocation_approvals`.
- `P4_MIGRATIONS` with `user_version = 4`.

## Must Hold

- `settled_at`/`settlement_kind` consistency CHECK.
- `side_effect_semantics` NOT NULL.
- Forward-only additive migration.

## Must Not Decide

- No P8/P10/P11 tables.
- No modification of 0001–0003.

## Acceptance

- Fresh DB migrates to 4.
- CHECKs reject inconsistent rows.

## Verification

```bash
pnpm test p4-ddl
pnpm check
```
