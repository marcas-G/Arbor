# P10-009 — Rebuild at-scale orchestration + checkpoints + projection-side retention

## Source

- P10 `04` 全部 (four handover items, materialized set, Inbox face); P9 `05` §3.2 + closure-reconciliation (floor−1 correction, DID v1.12 G2a); P1 `05` §4/§6 (apply-then-advance; floor/reset/replay); P10 `07` Story E

## Depends On

- P10-003, P10-004, P10-005, P10-007, P10-008

## Objective

Business-projection rebuild at scale (GQ2 handover from P9): incremental
checkpoints, per-projection retention, orchestration over the
P9-hardened generic rebuild, and per-view query-correctness assertions.

## Outputs

- Checkpointed catch-up per materialized projection; retention policies; rebuild orchestrator (triggers + ordering + drift reset); RB-4 per-view assertions; `p10-rebuild` suite.

## Must Hold

- **Checkpoints**: each business projection persists its applied-watermark; catch-up resumes from checkpoint + 1 — **never below the pruned floor**.
- **Triggers**: startup (after the P2 nine-step pass step 8), explicit operator command, detected drift (watermark < pruned floor ⇒ forced reset).
- **Rewind**: consumes the P9-closure corrected form **floor−1** (DID v1.12 G2a) unchanged — nothing further changed; ordering = **EffectiveFacts → raw fact views → Attention last**.
- **Materialized set** per `04` §2: Attention read-model rows, EffectiveFacts snapshot, Tree aggregates, Usage aggregates; `WaitingOnVacantProducer` is on-read (rebuild = re-derive, trivially correct); Inbox = state reconciliation (P10-008 face).
- **Query correctness (RB-4 restated)**: after any rebuild path, the projection reaches the same state as incremental application from the same events — per-view assertion.
- **Retention 投影侧 only**: projection rows / view lifetimes (transcript paging windows, superseded attention rows, consumed-inbox summary aging); **journal horizon 负断言** — journal pruning/horizon ownership stays P1 `04`, unchanged.
- No P9 generic-mechanism change — configure/use only; single-project scope.

## Must Not Decide

- No journal pruning/horizon change; no generic rebuild mechanism change; no cross-project rebuild; no canonical-row retention decisions.

## Acceptance

- Force-drift → orchestrated rebuild → per-view state == incremental replay from the same events; crash-mid-rebuild → checkpoint resume from checkpoint+1; retention assertions touch projection rows only; journal-horizon negative assertion green.

## Verification

```bash
pnpm test p10-rebuild
pnpm check
```
