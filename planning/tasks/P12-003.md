# P12-003 — SecretStorePort / SecretRef + real adapter

## Source

- docs/design/implementation/P12/03-secret-store.md §1–§4

## Depends On

- P11-013

## Objective

`SecretRef`/`SecretMaterial` opaque types, typed failures, `secret-env`/`secret-file` adapters, driver wiring, no-leak invariant over all durable/observable surfaces, sandbox env allow-list projection (mechanism).

## Outputs

`packages/ports` types + `adapters/secret-*`; suite `p12-secret-store`.

## Must Hold

- `03` §1–§4; no `SecretMaterial` in prompt/session/event/log/artifact (CI-2); adapter projects an env allow-list so sandbox cannot inherit material.
- Blocker: B5

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-4 mechanically checked (see `14-acceptance.md` §3.1/§3.2) ; blocker B5 evidenced.

## Verification

```bash
pnpm test p12-secret-store
pnpm check
```
