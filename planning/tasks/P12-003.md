# P12-003 — SecretStorePort / SecretRef + real adapter

## Source

- docs/design/implementation/P12/03-secret-store.md

## Depends On

- P11-013

## Objective

`SecretRef`/`SecretMaterial` opaque types, typed failures, `secret-env`/`secret-file` adapters, no-leak invariant over all durable/observable surfaces.

## Must Hold

- `03` §1-§4; no `SecretMaterial` in prompt/session/event/log/artifact (CI-2); sandbox env allow-list.

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- Owning contract assertions mechanically checked; any P12 completion blocker touched by this task is evidenced.

## Verification

```bash
pnpm test p12-secret-store
pnpm check
```
