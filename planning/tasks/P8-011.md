# P8-011 — Acceptance stories A–G + architecture + guards

## Source

- P8 `06` 全部; P5/P6/P7 suites (regression guards); DID v1.11 §10.4.1

## Depends On

- P8-006, P8-007, P8-008, P8-009, P8-010

## Objective

The seven deterministic acceptance stories (Fake Provider / direct command
submission, one durable DB, existing composition root) plus P8 architecture tests
and the P5/P6/P7 regression guards.

## Outputs

- `tests/acceptance/p8-*.test.ts` seven stories + `tests/architecture/` P8 edges.

## Must Hold

- **Story A — chain closure**: CompletionClaim settle → Consumer A (deterministic CommandId, replay idempotent) → spawn (ExecutionBound, no parent, workerKind Verifier) → Record ×2 → Conclude(Pass) via aggregation → Accept → Consumer B CompleteWork → `WorkCompleted` + atomic currentWorkId clear; revision constant; `(workId, revision, verificationId)` binding chain asserted; **Conclude(Pass) also releases channel-1 wake**.
- **Story B — FAIL rework, same person**: VerificationReturned wake asserted; same-Producer context (no new Work / no personnel change); Verifier's L5 write attempt on Producer formal artifacts rejected.
- **Story C — UNKNOWN first-class**: aggregation Unknown; wake still VerificationReturned; no FAIL semantics, no rework branch, no auto-PASS.
- **Story D — revision invalidation**: PASS then RefineWork → old Verification/Acceptance retained but inapplicable; stale-revision AcceptWorkOutcome rejected; new revision re-chains.
- **Story E — mission/P7 integration**: placeholder mission typed rejection; RefineWork-supplied mission passes; deliverable `sourceWorkRevision` inconsistency rejected; executable mission → environmentRevision required.
- **Story F — program gates**: P9 + P14 hash/tamper/version gates; P14 read-only negative.
- **Story G — owner snapshot**: snapshot recorded; retirement never auto-aborts (natural conclusion); impossible → Unknown(Orphaned) governance conclusion (payload carries conclusionReason) → re-Start with new identity + current owner.
- Supplementary: same-revision second StartVerification → `VerificationAlreadyOpen` typed rejection.
- Mechanical: five-command event/rejection sets = contracts; verdict immutable; verifier-only authority violations rejected; consumers never bypass handlers (fingerprint causal chain).
- Architecture: verification-runtime placement (deps = domain/ports/application); consumer → gateway only; package boundaries per §10.4.1.
- P5/P6/P7 regression guards all green, unchanged.

## Must Not Decide

- No real models/humans as gating; no P9+ phase assertions; no UI acceptance (P10).

## Acceptance

- Stories A–G green in one durable-DB run; `pnpm architecture` green.

## Verification

```bash
pnpm test p8-acceptance
pnpm test p8-architecture
pnpm architecture
pnpm check
```
