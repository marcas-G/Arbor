# P4-002 — `ports` P4 contracts

## Source

- P4 `01` (all), `02` §2, `03`–`07`

## Depends On

- P4-001

## Objective

Declare the P4 Effect service contracts and port errors.

## Outputs

- `ToolDefinition`, `ToolIntent`, `ToolExecutionContext`, `CanonicalToolObservation`, `SideEffectSemantics`, `ToolInvocationSettlement`.
- `ToolRuntimePort`, `ToolDefinitionStore`, `SandboxPort`, `ToolInvocationStore`, `ArtifactService`, `BlobStorePort`, `ArtifactMetadataRepository`, `ResourceAdmissionService` (the P2 `ReconciliationSource` contract already exists; P4-016 implements it).
- P4 port errors.

## Must Hold

- No universal error; each port narrow E.
- `CanonicalToolObservation.Denied` is the denial projection.
- No PermissionGrant resolver.

## Must Not Decide

- No adapter imports in ports.
- No behavior.

## Acceptance

- A/E/R match the contracts; typecheck green.

## Verification

```bash
pnpm test p4-ports
pnpm check
```
