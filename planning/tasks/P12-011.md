# P12-011 — Providers / tools breadth

## Source

- docs/design/implementation/P12/12-providers-tools.md §1–§6

## Depends On

- P12-001, P12-003, P12-007

## Objective

Real provider adapter (`adapters/provider-openai`), model catalog/model→adapter resolution, real `ModelCapabilityPort`, `ProviderFailureKind` closed-union disposition, non-builtin tools via the generic seam.

## Outputs

`adapters/provider-openai` + `packages/provider-runtime`/`model-context` catalog + `ALLOWED_EDGES` entry; suite `p12-providers-tools`.

## Must Hold

- `12` §2–§6; `ProviderPort`/`CanonicalProviderEvent` unchanged; P4 G1–G6 preserved; SPI MAJOR rules; Project tools via `01` §5 registration.

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- EC-12 mechanically checked (see `14-acceptance.md` §3.1/§3.2).

## Verification

```bash
pnpm test p12-providers-tools
pnpm check
```
