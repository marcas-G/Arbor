# P12-011 — Providers / tools breadth

## Source

- docs/design/implementation/P12/12-providers-tools.md

## Depends On

- P12-003, P12-007

## Objective

Real provider adapter (`adapters/provider-openai`), model catalog/model→adapter resolution, real `ModelCapabilityPort`, `ProviderFailureKind` closed-union disposition, non-builtin tools via the generic seam.

## Must Hold

- `12` §2-§6; `ProviderPort`/`CanonicalProviderEvent` unchanged; P4 G1-G6 preserved; SPI MAJOR rules.

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- Owning contract assertions mechanically checked; any P12 completion blocker touched by this task is evidenced.

## Verification

```bash
pnpm test p12-providers-tools
pnpm check
```
