# P6-004 — Approve consumer → CreateChildWorkspace/AssignWork (D1)

## Source

- P6 `01` §4.2; DID §5.4（Event→Command deterministic CommandId）; P1 `01` §6/§8

## Depends On

- P6-003

## Objective

Wire the Application consumer: `DecisionRecorded(approve)` → `CreateChildWorkspace`（→ `AssignWork` if initialWork）via `CommandGateway`.

## Outputs

- Consumer registration + deterministic command-id derivation.

## Must Hold

- CommandId = f(proposalId, proposalRevision); replay yields existing Receipt（P1 §8）.
- All P1 §6 preconditions still enforced; governance fact is no authority exemption.
- Human-gate observation（含 reject）returns to the originating Workspace Inbox.

## Must Not Decide

- No new commands; no P1 handler changes.

## Acceptance

- Approve ≠ create observable as two steps; duplicate delivery creates exactly one Workspace.

## Verification

```bash
pnpm test p6-formation-consumer
pnpm check
```
