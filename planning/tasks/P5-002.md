# P5-002 — Provisional `RunnableWorkSource` implementation

## Source

- P5 `02`; DID v1.9 §8.18A, G2

## Depends On

- P5-001

## Objective

Implement the provisional single-workspace `RunnableWorkSource` (P2 port).

## Outputs

- `RunnableWorkSourceLive` classifying `current` + `runnable` for one workspace.

## Must Hold

- Implements the existing P2 port; does not change it.
- `current` = workspace.currentWorkId when Open; `runnable` = Open Work minus current; stable order.
- No dependency/global runnability.

## Must Not Decide

- No port change.
- No dependency graph (P7).

## Acceptance

- Classification drives the P2 §8.18A table deterministically.

## Verification

```bash
pnpm test p5-runnable-source
pnpm check
```
