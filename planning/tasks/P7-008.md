# P7-008 — Classification single authority (supersedes P5 source)

## Source

- P7 `03` §1–§5; DID v1.10 §8.18A, G3; SD v1.3 §5.2, No.20/53

## Depends On

- P7-002

## Objective

Supersede the P5 provisional `RunnableWorkSource` implementation with the dependency-aware one; migrate wait-awareness from `decide()` inputs to `classify` (inherited evolution).

## Outputs

- Updated `apps/single-workspace` runnable-source + `ExecutionSchedulerLive` dependency cleanup + updated P5 suite expectations.

## Must Hold

- Port signature unchanged (P2 `02` §7 frozen); classify is the single runnability authority.
- **Blocking rule (frozen)**: `blocking(d, w) :⇔ d.state = Unsatisfied ∧ d.consumerWorkId = w ∧ ∃ wait ∈ activeWorkWaits(w): wait.conditions ∋ DependencyChanged(d.dependencyId, _)` — any observedRevision; an Unsatisfied Dependency alone never blocks (waiting is a cognitive decision; WorkWait is the only wait registry).
- `current = Some(w)` only when `w ∈ Open ∧ ¬waiting(w)`; runnable excludes every waiting Work; stable `WorkId` ordering; single-snapshot read set.
- `decide()` code unchanged — input semantics only; `ExecutionSchedulerLive` drops WorkWaitStore from the reevaluate decision path (register/clear/dueTimers uses retained, P2 mechanism unchanged).
- classify is read-only: no WorkWait registration/clearing, no wake, no canonical writes, no model calls (No.53).

## Must Not Decide

- No port signature change; no scheduler decision-table change; no quality input (P8); no cross-workspace semantics.

## Acceptance

- Classification matrix (wait × dependency combinations): `decide()` output equals classify for all cells; P5 suite green under supersession semantics.

## Verification

```bash
pnpm test p7-classification
pnpm check
```
