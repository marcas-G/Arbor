# P12-013 — Architecture allow-list reconciliation + P11 closure-test amendments

## Source

- docs/design/implementation/P12/00-contract-index.md (G8/TR-1/TR-9)

## Depends On

- P12-001, P12-009

## Objective

Remove stale `verification-runtime` from `package-dag.ts`; relocate `fingerprintOf`→`ports` and replace `CommandRejection` with a ports-level type (adapters domain/ports only); amend `p11-closure.test.ts` (advanceAnchor absence + adapter edges); migrate renamed catalog call sites.

## Must Hold

- `01` §7; `05` §5.1; DID §10.4.1; recorded TR-1/TR-9/G8/DF-16; no semantic-changing relocation without governance.

## Must Not Decide

- Anything the owning contract's Must-Not-Decide section excludes; no P12 implementation before explicit authorization.

## Acceptance

- Owning contract assertions mechanically checked; any P12 completion blocker touched by this task is evidenced.

## Verification

```bash
pnpm test p12-closure
pnpm test p12-acceptance
pnpm check
```
